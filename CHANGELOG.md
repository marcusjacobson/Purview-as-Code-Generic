# Changelog

All notable changes to this repository are documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/) and this repository follows [Conventional Commits](https://www.conventionalcommits.org/en/v1.0.0/).
Entries are grouped by merge date in reverse chronological order. A `#NNN` reference points to the pull request (or issue) that introduced the change; GitHub renders it as a link.

## How this file is maintained

Every state-changing pull request adds its own entry here **as part of that PR** — never in a follow-up PR, never by a direct commit to `main`. `@artifact-resolver` does this during implementation; a human author does it before opening the PR. The merge-time gate is enforced by `@owner-approval` and by [`pre-commit.instructions.md`](.github/instructions/pre-commit.instructions.md) and [`pull-request.instructions.md`](.github/instructions/pull-request.instructions.md).

To add an entry:

1. **Date heading.** Find or create a `## YYYY-MM-DD` heading for the merge date (in the lab's same-day cadence, today's date). Newest date first — a new date heading goes directly below this section, above all older dates.
2. **Category.** Under that date, find or create the matching `### <Category>` subsection, mapping the PR's Conventional-Commit type: `feat` → Added, `fix` → Fixed, `refactor` / `chore` → Changed, `perf` → Performance, `revert` → Reverted, `docs` → Documentation, `test` → Tests, `ci` → CI/CD, `build` → Build; a deletion uses Removed. Keep categories in the order Added, Changed, Fixed, Removed, Performance, Reverted, Documentation, Tests, CI/CD, Build.
3. **Bullet.** Add `- **<scope>:** <subject> (#NNN)` at the top of that category, where `<scope>` is the commit scope, `<subject>` is the Conventional-Commit subject without its `type(scope):` prefix, and `#NNN` is the originating issue number. Historical entries reference the squash-merge PR instead; either renders as a link on GitHub.
4. **Exemption.** A PR whose only change is this file (a manual changelog fix) does not add an entry for itself. Pure upstream mirror-sync PRs that carry only upstream CHANGELOG entries and no repo-local changes may omit a new bullet (the upstream entries already document the imported changes).

## 2026-07-17

### Added

- **scripts:** `New-RepoLabels.ps1` — idempotent seeder for the full GitHub label set the automation depends on (#129). GitHub's "Use this template" does not copy labels, so every spin-off starts with the label-gated automation dormant: `pr-auto-merge.yml` / `@owner-approval` gate on `owner-approved` (observed live downstream as `'owner-approved' not found` at merge time), `@idea-intake` on `needs-review`, destructive PRs on `destructive`, issue triage on the five `squad:*` labels, and the watch loops on `code-currency` / `surface-watch` / `watch-list` / `drift-detected`. The seeder creates only missing labels (operator customizations survive re-runs), matching the create-only-missing idiom the `sync-*-from-tenant` drift steps already use; three labels — `owner-approved`, `destructive`, `surface-watch` — have no run-time self-seeding fallback, so this is the only thing that creates them. `New-RepoLabels.Tests.ps1` pins the set against the labels the committed workflows actually reference (coverage assertion goes red if a workflow gates on a label the seeder would not create; verified non-vacuous by red-replay). Kickoff/onboarding docs (`kickoff-guide.md`, `tenant-onboarding.md`, `@operator-kickoff` handoff) gain the seeding step.
- **scripts:** `-SubjectFormat auto|classic|immutable` on `New-AutomationEntraApp.ps1` / `New-KvUnlockEntraApp.ps1` — first-class support for GitHub's immutable (ID-embedded) OIDC subject claims per [ADR 0058](docs/adr/0058-immutable-oidc-subject-claims.md) (#126). Repositories created on or after 2026-07-15 (and repos renamed/transferred/opted-in after that date) mint `repo:<org>@<ownerId>/<repo>@<repoId>:environment:<env>`; the scripts previously built only the classic ADR 0010 subject and hard-refuse mismatches, so a post-cutoff spin-off either failed `azure/login` with `AADSTS700213` (classic credential) or failed every reconcile (hand-corrected immutable credential) — observed live on the downstream operations repo, where all six federated credentials had to be updated by hand. The scripts now resolve the repository's numeric identity at runtime (`gh api` with an unauthenticated `api.github.com` fallback), prefer the format the repository mints (creation-date heuristic; `immutable` override for renamed/transferred/opted-in repos; `classic` pins the old behavior offline), and accept either format on verification — with a shape-only pattern acceptance (warning, not refusal) when the IDs could not be resolved, and a loud dormancy warning for a classic credential on an immutable-minting repo. Every ADR 0010 invariant (single credential, environment-scoped subject, never-silently-reconcile) is unchanged; tests extended/added for both scripts (33 tests); `getting-started.md` §1/§2 document the two-format contract and the `gh api` ID-resolution commands.
- **infra:** `deployPurviewAccount bool` parameter (default `true`) makes the classic `Microsoft.Purview/accounts` resource conditional in `main.bicep` (#127). Operators on the tenant-level Unified Catalog experience ([ADR 0047](docs/adr/0047-unified-catalog-preview-api-coexistence.md)/[ADR 0048](docs/adr/0048-purview-account-discovery-gate.md) — no classic account exists, and a PAYG metering resource must never be targeted) can now run the canonical `az deployment group create -f infra/main.bicep` with `--parameters deployPurviewAccount=false` and still get the `role-definitions` module (the KV firewall-toggler role `kv-temp-unlock.yml` depends on); previously they had to deploy the module standalone, violating the canonical-commands rule (observed downstream). Outputs are `''` under the documented ternary guard (`systemAssignedPrincipalId` uses the null-forgiving operator for BCP318); ARM's `if()` evaluates only the matching branch, so the `reference()` never fires when the account is skipped; `main.json` regenerated with the same bicep 0.44.1.10279.

### Fixed

- **scripts:** shorten the `merge-commit` label description under GitHub's 100-character limit and add the length guard the test lacked (#132). #131 introduced a 143-character description; `gh label create` rejects a description over 100 characters with HTTP 422 and `New-RepoLabels.ps1` halts on the first failed create, so a fresh spin-off would have seeded labels only up to `merge-commit` and stopped — leaving `destructive` and every label after it uncreated (surfaced live while seeding the downstream repo). The description is trimmed under the limit, and `New-RepoLabels.Tests.ps1` now asserts every seeded description is at most 100 characters — closing the gap that let the over-long value pass CI green.
- **ci:** `pr-auto-merge.yml` selects the merge method instead of hard-coding `--squash` (#131). The auto-merge step always squashed, which silently violates [ADR 0057](docs/adr/0057-multi-environment-and-branch-model.md) section 9 for a shared-history PR: an upstream-template sync must land as a **merge commit**, because squashing rewrites the shared history the fast-forward-only mirror contract depends on. The step now defaults to squash but switches to `--merge` when the PR carries the new **`merge-commit`** label (rebase is never offered — it also rewrites that history); the confirmation comment and the linked-issue close comment become merge-method-agnostic. `New-RepoLabels.ps1` seeds `merge-commit` (13 labels now), and `New-RepoLabels.Tests.ps1`'s coverage assertion binds the workflow's `grep -qx "merge-commit"` reference to that seeder entry. `owner-approved`'s seeded description drops the stale "squash" wording.
- **ci:** the vault-open verify keys on `publicNetworkAccess`, tolerating an Azure-normalized `defaultAction` (#130). #128's "Verify the open actually stuck (read back)" step in `validate-oidc-auth.yml` and `kv-temp-unlock.yml` asserted `networkAcls.defaultAction == "Allow"`, but `az keyvault update --public-network-access Enabled --default-action Allow` makes Azure normalize an all-Allow / no-rules ACL to `null` — read back as `"None"` via `-o tsv`, and the open command's own `-o json` returns `{"pna":"Enabled","da":null}`. So the check false-positived on every NORMAL open: it failed the run, skipped the data-plane proof, and mislabeled the state an Azure Policy `modify` defeat — observed live on the downstream **non-governed dev** tenant, where #128 regressed a `validate-oidc-auth` run (`29584015269`) that was green immediately before the merge. The verify now gates on `publicNetworkAccess` (the layer the open actually flips): `Enabled` with `defaultAction` `Allow` **or** `None`/null verifies open; `publicNetworkAccess != Enabled` keeps the policy-`modify` diagnosis (the real modify signature, e.g. an MG-scoped `KeyVault_PublicNetwork_Modify`); a *persisting* `defaultAction=Deny` under `Enabled` fails with its own targeted "still Deny" message. New `tests/workflows/KeyVaultOpenVerify.Tests.ps1` red-replays the extracted verify block from both shipped workflows across all four states (10 assertions).
- **ci:** harden the Key Vault firewall open steps against the two silent-failure modes observed on the first live downstream runs (#128). (1) `validate-oidc-auth.yml`'s open flipped only `publicNetworkAccess`, so an adopted vault carrying `networkAcls.defaultAction: Deny` with no `ipRules` stayed unreachable after a "successful" open — data-plane calls failed with "(Forbidden) Client address is not authorized and caller is not a trusted service" while PNA read Enabled; the open now sets both layers (`Enabled`/`Allow`) and the close restores `Disabled` plus the vault's **pre-open** `defaultAction`, so the workflow never changes steady-state posture (hardened/adopted vaults stay `Deny` — the posture `kv-temp-unlock`'s pre-unlock guard requires; bootstrap vaults stay `Allow`, which the per-solution data-plane workflows' PNA-only opens depend on). (2) An MG-scoped Azure Policy `modify` effect can rewrite the open PUT's value inside the same operation — `az keyvault update` exits 0 and every later data-plane step fails mysteriously; **both** `validate-oidc-auth.yml` and `kv-temp-unlock.yml` now read the state back in a dedicated verify step and fail loudly with a policy-diagnosis hint (Activity Log signature `Microsoft.Authorization/policies/modify/action` + runbook pointer). The verify is deliberately a separate step from the open so a failed verify still triggers the `always()` re-lock — the vault is never left half-open. New runbook section "Governed tenants" documents the bounded single-vault Waiver exemption (pinned via `--policy-definition-reference-ids`, `--expires-on` as the self-expiring backstop since a `CanNotDelete` lock blocks exemption deletion) that ran successfully downstream; `tenant-onboarding.md` gains the adopted-vault firewall-posture section. All bash run blocks in both workflows shellcheck-clean (v0.10.0, 0 findings).
- **infra:** grant `Microsoft.KeyVault/vaults/read` to the KV firewall-toggler custom role in `role-definitions.bicep` (#125). The role granted only `vaults/write`, but `kv-temp-unlock.yml`'s "Pre-unlock state guard" and "Assert final vault state is locked" steps run `az keyvault show`, which needs `vaults/read` — so the workflow failed at the guard with `AuthorizationFailed` after a successful `azure/login`. `docs/runbooks/kv-temp-unlock.md` already documented the role as read + write; the bicep never caught up, and the template repo (no tenant credentials by design) could never exercise the path — the downstream operations-repo run was its first live execution. The action list converges on exactly `['Microsoft.KeyVault/vaults/read','Microsoft.KeyVault/vaults/write']`, the list already hot-fixed into both downstream tenants via `az role definition update`, so a future module redeploy is a no-op on permissions, not a revert. `infra/main.json` regenerated with the same bicep 0.44.1.10279 that generated it; `New-KvUnlockRbac.ps1` header prose ("the single action") corrected in lockstep.

### Documentation

- **docs:** ADR 0058 — federated-credential subjects support GitHub's immutable (ID-embedded) OIDC subject format; classic + immutable become a two-format contract with the immutable form preferred wherever GitHub mints it (#126).
- **docs:** define the `PURVIEW_ACCOUNT_NAME` Environment-variable contract for unified-only operators: **omit the variable** (#127). Verified against current main: no workflow consumes or guards it — each workflow's fail-fast guard checks exactly the variables that workflow consumes, so `validate-oidc-auth.yml` running green without it is by design; the variable feeds only the classic reconcilers' `${env:PURVIEW_ACCOUNT_NAME}` tokens via `Resolve-EnvTokens.ps1` (ADR 0023 Category 2, named unset-token error — fail-closed on a tenant classic reconcilers cannot drive anyway), matching the ADR 0048 outcome matrix (placeholder stays on confirmed-unified tenants). Corrects the docs that overstated the guard behavior (`getting-started.md` §2, `tenant-onboarding.md` Step 5, `kickoff-guide.md` §4, `github-actions.instructions.md`, `operator-tenant.agent.md`); adds the unified-only deploy path to `getting-started.md` §3 and the canonical account-less command to `build-deploy.instructions.md`.

## 2026-07-16

### Changed

- **scripts:** `New-KvUnlockRbac.ps1` resolves the firewall-toggler role name from `automation.kvFirewallTogglerRoleName` in the parameters file (or `-RoleName`), closing the ADR 0057 §6 gap where Bicep parameterized the name but the grant script hardcoded `Purview-Lab-KV-Firewall-Toggler` (#123).
- **scripts:** `New-AutomationEntraApp.ps1` and `New-KvUnlockEntraApp.ps1` treat federated-credential **name** as advisory when issuer/subject/audiences match — supports repository-migration cutover with a temporary name like `gh-env-lab-ops` (ADR 0057 §7 step 4).
- **docs:** operator-branch model in `@operator-tenant`, greenfield bootstrap sequence in `automation-identity.md`, GitHub plan prerequisite for required reviewers on private repos in `getting-started.md` (#123).

### Added

- **ci:** first-class multi-environment support — `lab` + optional `dev` GitHub Environments from one workflow set ([ADR 0057](docs/adr/0057-multi-environment-and-branch-model.md)) (#123). All 16 tenant-touching workflows (`deploy-infra`, `validate-oidc-auth`, `kv-temp-unlock`, 5× `deploy-<solution>`, 5× `sync-<solution>-from-tenant`, `drift-detection`, `export-content-explorer`, `code-currency-watch`) gain an `environment` dispatch input (choice `lab` | `dev`, default `lab`); push/schedule runs map branch `dev` → `dev` and every other branch → `lab` via the byte-identical canonical expression `${{ inputs.environment || (github.ref_name == 'dev' && 'dev' || 'lab') }}`. Jobs declare the selected Environment so the OIDC subject stays `repo:<org>/<repo>:environment:<environment>` (ADR 0010); `kv-temp-unlock` pairs `lab` → `kv-unlock`, `dev` → `kv-unlock-dev` on both jobs; concurrency groups are environment-derived with `cancel-in-progress: false` retained; tenant-specific non-secret literals moved to Environment variables (`PURVIEW_RG`, `TENANT_DOMAIN`, `DATA_PLANE_CERT_NAME` join `KEY_VAULT_NAME` / `PURVIEW_ACCOUNT_NAME`) with fail-fast guards; `deploy-infra` selects `infra/main.<environment>.bicepparam` for non-lab environments (fails fast when missing); drift-back branches are environment-suffixed (`auto/<surface>-drift-<env>`) with the PR base following the triggering branch. `validate.yml` triggers widen to `[main, dev, lab]` so operator branches run all ten gates.
- **scripts:** honor the `PURVIEW_PARAMETERS_FILE` environment variable when `-ParametersFile` is omitted (#123). All 32 scripts that defaulted to `infra/parameters/lab.yaml` now resolve `$env:PURVIEW_PARAMETERS_FILE` first (workflows set it to `infra/parameters/<environment>.yaml`), falling back to the shipped lab file; an explicit `-ParametersFile` still wins, so single-environment spin-offs are unchanged.
- **infra:** parameterize the Key Vault firewall-toggler custom role name (`kvFirewallTogglerRoleName`, default `Purview-Lab-KV-Firewall-Toggler`) in `role-definitions.bicep` with a `main.bicep` pass-through (#123). The default preserves existing deployments; because the deterministic role-definition GUID is seeded from the name, overriding it mints a NEW role definition — the four-step migration (deploy new, re-run `New-KvUnlockRbac.ps1`, verify via `kv-temp-unlock`, delete old) is documented in ADR 0057 §6.

### Fixed

- **scripts:** file-hygiene fix-forward for the ADR 0057 execution-gap commit — no semantic change (#123). (1) The new federated-credential name-advisory strings carried non-ASCII characters (`§`, em-dash) in the BOM-less `New-AutomationEntraApp.ps1` / `New-KvUnlockEntraApp.ps1`, so PSScriptAnalyzer's `PSUseBOMForUnicodeEncodedFile` turned the `powershell` validate gate RED on every branch carrying that commit; the characters are replaced with ASCII (`section 7`, `--`), restoring both files' prior ASCII-only/no-BOM state. (2) `New-KvUnlockRbac.ps1` was committed with one CRLF line inside an otherwise-LF blob (violating `* text=auto eol=lf` — the mixed index blob makes the file appear permanently modified on every fresh checkout, in every clone including downstream operator repos) plus a newly added UTF-8 BOM; the blob is re-staged LF-only via `git add --renormalize` and the BOM dropped (file is ASCII-only). Fixed forward with a new commit per ADR 0057 §9 — no amend/rebase/squash, since downstream branches already build on the shared history. A new guard, `tests/repo/IndexEolHygiene.Tests.ps1`, fails the pester gate on any future mixed-EOL index blob (`git ls-files --eol`-based).

### Documentation

- **docs:** ADR 0057 — multi-environment support, the operator branch model (`main` upstream mirror / `dev` / `lab`), per-environment configuration by copying the lab files (no shipped `dev` scaffolds), and the ADR 0010 bounded federated-credential repository-migration cutover (add the new repo's subject, verify via `validate-oidc-auth` from the new repo, remove the old credential within the same change window; `New-AutomationEntraApp.ps1` intentionally keeps failing on >1 credential — the anomaly detection is preserved, not weakened) (#123). Rewrites the `copilot-instructions.md` "Environment and identifier boundaries" section for the two-environment contract (identifier rules unchanged); updates `github-actions.instructions.md` (environments, secrets, concurrency), `build-deploy.instructions.md` (per-environment `.bicepparam` selection), `getting-started.md` (Environment variables incl. `PURVIEW_RG` / `TENANT_DOMAIN` / `DATA_PLANE_CERT_NAME`, optional `dev` setup with `dev` / `kv-unlock-dev` federated subjects), and `tenant-onboarding.md` (optional multi-env step). Retires the `.github/workflows/**` tenant-surface entry in `tenant-placeholders.yaml` as an edit surface (literals moved to Environment variables) while keeping `functionalWorkflowScan` as the zero-match regression guard.

### Tests

- **tests:** `tests/workflows/EnvironmentRouting.Tests.ps1` pins the ADR 0057 routing contract across all 16 tenant-touching workflows — dispatch-input shape, byte-identical environment expressions (canonical / dispatch-only / kv-unlock pairing on both jobs), environment-derived concurrency with `cancel-in-progress: false`, and zero functional tenant literals in `.github/workflows/**`; guards the YAML 1.1 gotcha where the bare key `on` parses as boolean true (#123). The `ShippedDesiredState` suites become branch-aware: the ADR 0056 emptiness assertions (empty root lists, shipped `mode: Enable`, CLAIM 1 no-op proofs) skip with a printed message only when validation targets `dev` or `lab` (operator branches carry populated desired state by design) and stay enforced for `main` and every other branch; all every-copy-forever assertions (parse integrity, carve-out pins, no raw principal GUIDs, `examples/**` inert) run everywhere.

## 2026-07-15

### Fixed

- **ci:** derive `issue-triage.yml`'s squad-label routing from the issue body's own `## Squad Personas` section, keyword-grep as fallback only (#117). The workflow's own comment (`:50-52`) claimed *"the primary persona is determined by the issue body's `## Squad Personas` section"*, but the code below it never parsed that section at all — it ran 5 independent, non-exclusive `grep -qE` keyword checks against one lowercased `title + body` blob, each stamping a label on any match, because common boilerplate words (`test`, `document`, `policy`, `script`) that appear in nearly every issue's own Acceptance-Criteria/References sections tripped most of the 5 categories regardless of relevance — confirmed saturating on issues filed this session (#105 got 6 labels, #106/#108 got 5, #103 got 3). **Fix:** extract the text strictly between the `## Squad Personas` heading and the next `## ` heading (or end of body) via `awk '/^## Squad Personas/{flag=1; next} /^## /{flag=0} flag'`, then pull every backtick-quoted `` `squad:[a-z-]+` `` token out of it (`grep -oE` + strip backticks + `sort -u`) — the label slug is already embedded in each Primary/Supporting line, so no persona-name-to-label mapping table is needed. If at least one token is found, apply exactly those labels via a single `gh issue edit --add-label <csv>` call and **skip the keyword-grep entirely** (`exit 0` before it runs); this is what stops the grep from re-adding labels the Squad Personas section deliberately left out. If no section exists, or the section exists but carries no `squad:*` token (a hand-edited/malformed variant), fall back to the original keyword-grep, byte-for-byte unchanged — this preserves current routing for issues filed directly via `gh issue create` rather than through `@idea-intake` (confirmed shape-checked against #108, #112, #114, #115, none of which carry the section). **Verified by a bash red-replay harness**, not narrative claim: both routing paths were extracted into standalone scripts (stdout-instrumented in place of `gh issue edit` calls, plus a stderr marker proving the keyword-grep branch is structurally skipped, not merely coincidentally silent) and driven against three bodies. (1) A synthetic saturation-shaped body carrying keywords for all 5 categories (architecture/ADR, security/policy, script/automation, test/verify, document/runbook) plus a `## Squad Personas` section naming 2 personas: OLD produced all 5 labels (`squad:lead-architect`, `squad:security-specialist`, `squad:automation-engineer`, `squad:tester-validator`, `squad:scribe`); NEW produced exactly the 2 named (`squad:automation-engineer`, `squad:tester-validator`), with the keyword-grep marker absent. (2) The real `#108` body (no `## Squad Personas` section): OLD and NEW both produced the identical 4-label set (`squad:lead-architect`, `squad:security-specialist`, `squad:automation-engineer`, `squad:tester-validator`), proving the fallback is unchanged. (3) A synthetic hand-edited body with the heading present but its backtick tokens stripped: both OLD and NEW fell back to the same 3-label keyword-grep set rather than silently producing zero labels. Additional robustness checks: an empty issue body, a `## Squad Personas` section with Primary only (no Supporting line), a section with 3 comma-separated Supporting personas, and CRLF line endings were all extracted correctly. **The PR's own `validate.yml` actionlint job (`ubuntu-latest`, real `shellcheck` on `PATH`) caught a real finding a local-only pre-push check missed**: `SC2016` on the `SQUAD_LABELS=` line, because the backtick-quoted `` `squad:[a-z-]+` `` grep pattern's literal backticks (matching the Markdown code-span GitHub renders around a label token) look like unintended command-substitution syntax inside single quotes to shellcheck's heuristic, even though they are correct as written. Suppressed with a `# shellcheck disable=SC2016` directive and an inline comment stating the reason, matching the exact carve-out this repo already uses for the same rule in `pr-auto-merge.yml` (#115). Verified locally after downloading `shellcheck` v0.10.0 (this machine had none on `PATH`, the same gap #115/#116 documented): shellcheck flags the line without the directive and is clean with it, against the run-block extracted from the actual committed file with `${{ }}` GitHub Actions expressions neutralized the way actionlint's own integration does before delegating to shellcheck. `actionlint` v1.7.12 (cached from #116) itself reports 0 findings against both the edited file alone and the full 27-file `.github/workflows/**` tree (its own shellcheck integration hung against this local Windows/Git-Bash environment for an unrelated reason — the subprocess pipe never returned — so the `SC2016` finding was caught by the PR's CI run, not the local actionlint pass; the direct-shellcheck check above closes that gap). `yaml.safe_load` parses the file cleanly. Does not touch `needs-review` labeling (`idea-intake-autoadd.yml`, out of scope) or the "Ensure squad labels exist" step.

- **ci:** close a script-injection risk in `pr-stacked-retarget.yml` and add actionlint to `validate.yml` as a blocking check (#115). `github.event.pull_request.head.ref` — the PR author's own, fully attacker-controlled branch name on this public repo — was spliced directly into a `run:` script's text (`MERGED_HEAD="${{ github.event.pull_request.head.ref }}"`), which GitHub Actions substitutes as raw text **before** the shell parses it: the classic script-injection pattern a shell script itself cannot defend against. Fixed by moving the value into the step's existing `env:` block instead, so the substitution happens through the process environment, outside the script's syntax. **A second, related exposure of the same tainted value was found and fixed in the same PR, not by actionlint**: the follow-on "Retarget stacked PRs" step re-interpolated the value a second time via `${{ steps.find-stacked.outputs.merged_head }}` — actionlint's expression checker does not taint-track through step outputs, so it never flagged this occurrence, but the underlying attacker-controlled string is unchanged by having passed through `GITHUB_OUTPUT`, so it was moved to `env:` there too. **Catalog, re-verified fresh rather than trusted from the issue**: actionlint v1.7.12 against the full 27-file `.github/workflows/**` tree, run on a machine with no `shellcheck` binary installed, found exactly the one flagged occurrence (line 25/26, matching #115's own catalog) before this PR, and 0 findings after — matching #115's own catalog basis exactly. Added a new `actionlint` job to `validate.yml` as a normal blocking check (no `continue-on-error` — the near-zero-noise catalog left nothing to phase in), matching this file's existing "download/install the tool, then run it" idiom (`az bicep install` / `pip install yamllint` / `Install-Module PSScriptAnalyzer`) rather than a marketplace action or Docker image: it downloads the official `rhysd/actionlint` release binary via the project's own documented install script and runs it with no path arguments, matching [actionlint's own documented GitHub Actions usage](https://github.com/rhysd/actionlint/blob/main/docs/usage.md#use-actionlint-on-github-actions). **The new job's own first run, on this PR, failed** — actionlint's shellcheck integration is silent unless a `shellcheck` binary is present on `PATH`, present-but-unnoticed here because the `ubuntu-latest` GitHub-hosted runner ships `shellcheck` preinstalled while the machine used for the pre-push local catalog did not, so two additional, pre-existing findings in **files outside #115's declared 3-file scope** surfaced only once the job actually ran in CI: `pr-owner-gate.yml:53` (SC2129 style nit — three individual `>> "$GITHUB_OUTPUT"` redirects building a multiline output, grouped into a single `{ …; } >> "$GITHUB_OUTPUT"` block, no behavior change) and `pr-auto-merge.yml:225` (SC2016 — a single-quoted `gh api graphql` query body, flagged because it contains `$owner`/`$name`/`$number`; these are **GraphQL's own** variable-declaration syntax, not shell variables, and are deliberately kept un-expanded by the shell — their real values already flow in separately via the adjacent `-f owner=`/`-f name=`/`-F number=` flags, so shellcheck's suggested double-quote fix would have broken the query; suppressed with a `# shellcheck disable=SC2016` directive and an inline comment stating the reason, per this repo's carve-out discipline). Installing `shellcheck` v0.10.0 locally reproduced both findings exactly, confirming CI parity; after both fixes, the same local run — now with the same `shellcheck` coverage the CI runner has — reports **0 findings**, and the new job passed on its own PR (this PR: expanded to 5 touched files — `validate.yml`, `pr-stacked-retarget.yml`, `pr-owner-gate.yml`, `pr-auto-merge.yml`, `CHANGELOG.md` — because turning on a repo-wide, blocking linter surfaces whatever it finds repo-wide, and leaving a newly-blocking check permanently red on `main` over two small, unrelated, already-understood findings was worse than fixing them in the same PR).

- **ci:** fix `surface-watch.yml`, invalid YAML that has never once parsed (130/130 recorded runs were `push`-attributed workflow-file-validation failures; zero `schedule` events ever registered). The `run: |` block scalar for the "Parse §3 inventory and fetch Learn pages" step has a 10-space indentation floor, but the here-string closing delimiter `$issueBody = @" ... "@` at line 170 sat at column 0 — PowerShell here-strings require the closing `"@`/`'@` to have **zero** leading whitespace (confirmed empirically: `White space is not allowed before the string terminator`), which is structurally incompatible with any nonzero YAML block-scalar floor. Reindenting the closing delimiter to satisfy YAML would have broken the PowerShell parse instead; the fix replaces the here-string with a `[System.Text.StringBuilder]` (`[void]$sb.AppendLine(...)`), the idiom this repo already uses in `code-currency-watch.yml` and `watch-list-review.yml` for exactly this reason. Content-preserving mechanical rewrite only — same headings, same conditional `### Features…` sections, same "Next steps" list; the fetch/parse logic (`:54-160`) and the sibling "Create surface-watch issue" step that reads `surface-report.txt` back are untouched. **Verified in three gates**: (1) `yaml.safe_load` parses the whole file cleanly; (2) both `run:` blocks, extracted from the *parsed* YAML structure (not hand-dedented), pass `[System.Management.Automation.Language.Parser]::ParseInput()` with 0 `ParseErrors`; (3) the old here-string logic and the new StringBuilder logic were extracted as standalone functions and driven with three synthetic inputs (one report populated, all three populated, all empty) — outputs are byte-identical once normalized for one named, justified delta: `StringBuilder.AppendLine` emits `[Environment]::NewLine`, which is CRLF on the Windows machine used to run this test harness but LF on the `ubuntu-latest` runner the workflow actually executes on (matching the old code's hardcoded `` `n ``), so the two are byte-identical in production with no normalization needed. The report-building code still only runs inside `if ($driftDetected) { ... }`, unchanged. Cron cadence unaffected: weekly Monday 03:00 UTC (`0 3 * * 1`). PSScriptAnalyzer: no new findings (pre-existing `PSAvoidUsingWriteHost` warnings are inherited from unedited lines and are this repo's standard GitHub Actions logging idiom, not a regression). (#112)

- **scripts:** stop `Deploy-UnifiedCatalog.ps1` from deleting tenant objects under `-DirectionPolicy audit -PruneMissing` (ADR 0029 violation). **Found by the [#83](https://github.com/marcusjacobson/Purview-as-Code/issues/83) PR-B batch-2 agent while gating the eleven Class A reconcilers, and already annotated in-source as "a pre-existing ADR 0029 defect, reported separately and NOT fixed here" — this PR resolves it.** `Invoke-DirectionPolicyPlan`'s audit short-circuit clears `$plan` and returns, but `$orphans` is a **separate** top-level list, populated by six independent `$orphans.Add(...)` call sites **before** `Invoke-DirectionPolicyPlan` is ever called, and the function is never passed it — it has no way to reach it. Nothing else in the script flipped `$WhatIfPreference`, so the `-PruneMissing` delete loop (`foreach ($entry in $orphans.ToArray())`, each write wrapped in its own `$PSCmdlet.ShouldProcess(...)`) still walked a fully-populated `$orphans` and deleted every orphan, while the script's own log line claimed *"no writes would have fired."* **Fix: flip `$WhatIfPreference = $true` at the call site** — the identical mechanism `Deploy-Collections.ps1` already uses (and 8 further Class A reconcilers: DataSources, DLPPolicies, FilePlan, Glossary, IRMEntityLists, IRMPolicies, RetentionPolicies, Scans) — so every `$PSCmdlet.ShouldProcess()` call for the rest of the run, both the create/update loop and the delete loop, renders a "What if:" preview instead of writing; `$orphans` itself is left fully populated so the preview stays accurate. **Verified by RED-REPLAY, driving the real committed AST, not a reimplementation**: extracting the actual top-level delete-loop and write-loop statements from the pre-fix script, stubbing only the process boundary (`Invoke-UC*Delete`/`Invoke-UC*Create`, `$PSCmdlet`), and driving them with `-DirectionPolicy audit -PruneMissing` fired **2/2** stub deletes and **1/1** stub create; the identical harness against the fixed script fires **0/0/0** and takes the "What if:" preview path instead — locked in as permanent regression coverage in `tests/scripts/Deploy-UnifiedCatalog.Tests.ps1`, which goes red against the pre-fix script and green after. ⚠️ **Intentional side effect, not a regression**: the ADR 0052 prune gate's `IsWhatIf` argument (`[bool]$WhatIfPreference`) is captured *after* this fix runs, so under audit mode the gate now also stays silent — correct, because there is nothing left to confirm. Before this fix, an operator who answered "yes" (or passed `-Force` / `-Confirm:$false`) at that prompt still got real deletes; after this fix, there is no prompt to answer. **Regression-checked**: `portal-wins` / `repo-wins` behaviour is unchanged (the inserted block only fires under `audit`) — `Deploy-UnifiedCatalog.Tests.ps1` (28/28, up from the pre-fix baseline of 25/25 plus the 3 new tests) and `ConfirmGate.Tests.ps1` (411 passed / 24 skipped / 435 total, byte-identical to the pre-fix baseline — this PR does not touch `$orphans`' lifecycle, `$script:RepoWinsOverwrites`, or the gate's wiring/class/`ConfirmImpact`) both confirmed unchanged. Full repo Pester suite green (1999 passed, 0 failed, 24 skipped); PSScriptAnalyzer clean on `scripts/Deploy-UnifiedCatalog.ps1`, and the 3 pre-existing findings on `tests/scripts/Deploy-UnifiedCatalog.Tests.ps1` (an established `$DirectionPolicy`-unused-variable false positive, unrelated to this change) are unchanged. References [ADR 0029](docs/adr/0029-source-of-truth-direction-policy.md) and [ADR 0052](docs/adr/0052-destructive-confirmation-gate-at-script-layer.md). (#106)
- **scripts:** restructure `Deploy-EntraDirectoryRoles.ps1` to a two-phase (plan-then-apply) reconciler and gate its `-PruneMissing` revoke branch per ADR 0052, closing the **last** declared exception in `tests/scripts/ConfirmGate.Tests.ps1`'s `$script:UngatedByDesign` table. **This was deliberately split out of [#83](https://github.com/marcusjacobson/Purview-as-Code/issues/83) PR-B by owner ruling, because it is a genuine behaviour change on a PERMISSIONS surface, not a mechanical gate rollout.** Its reconcile loop was single-pass and **interleaved**: for each `(role, scope)` row it read current assignments, then immediately **POSTed creates** and **DELETEd revokes**, then moved to the next row — so row 2's revoke plan did not exist until row 1's writes had already landed. There was no point in that loop where "no tenant writes were made" was true and the full plan was known, so it could not take the ADR 0052 gate (whose decline branch is contractually required to make that phrase **true**) without a restructure; gating it in place would have shipped a gate whose abort message was **a lie**. **The fix mirrors the in-repo reference, `Deploy-PurviewRoleGroups.ps1`, minus its Phase 2**: Phase 1 now reads and categorizes every row — resolving role definitions, validating members, diffing tenant vs. desired state, and accumulating a per-row plan entry (role, scope, resolved role-definition id, validated Create object IDs, and Revoke object IDs paired with their tenant assignment IDs, since Phase 3's `DELETE` addresses an assignment by its own id, not by principal) — with **zero** tenant writes; the ADR 0052 gate then prompts once, keyed on the flattened Revoke union across every row (a plan predicate, never a policy one — this script declares no `-DirectionPolicy` at all, Class B, exactly one gate); Phase 3 applies that **same plan** Phase 1 built. **No Phase 2 reconnect step was added**, unlike the RoleGroups reference: that script's Phase 2 rebinds a Security & Compliance PowerShell session that degrades under high read volume, and this script authenticates via a single Graph REST access token acquired once, which has no analogous degradation — inventing a reconnect step it does not need was considered and rejected. `ConfirmImpact` raised `'Medium'` → `'High'`; `[switch]$Force` was already declared; report row content, categories (`NoChange`/`NoOp`/`Create`/`Revoke`), and the two idempotent-conflict catch blocks (`'A conflicting object…'` downgrading a Create to `NoChange`; `'Resource…does not exist'` downgrading a Revoke to `NoChange`) are preserved verbatim, only relocated so Create/Revoke rows emit adjacent to their actual write in Phase 3. **Verified by the decisive property this fix creates, not merely "does the gate exist"**: a harness extracting the committed Constants/Helpers/Apply-mode source verbatim (network boundary and the gate itself are the only two things stubbed) drove a two-row plan where **both** rows carry a Create **and** a Revoke, declined the gate, and confirmed **zero** POST and **zero** DELETE calls fired for **either** row — including row 1, which the old interleaved code would already have written by the time any gate could exist — while a positive-control consent run on the identical plan produced the expected 2 creates + 2 revokes, and two further runs confirmed the gate stays silent both without `-PruneMissing` and on an empty revoke plan. A B2-shaped mutant (population of the gate's revoke list moved behind an unrelated `-Force` conditional instead of the actual diff) was injected into the committed script, confirmed to flip `ConfirmGate.Tests.ps1`'s rule (c) append-only check **red** by name, then reverted — rules (a)/(b) do not apply here (Class B has no overwrite gate), and rule (c) passes by construction: the gate's list is built directly from `$plan`, the same collection Phase 3 iterates, with no separate shadow list and no lifetime between construction and read, so the B2 divergence this repo has shipped before is structurally impossible on this branch. `tests/scripts/ConfirmGate.Tests.ps1`'s CI-hang gated-script floor moves `21` → `22`; the completion criterion now covers **all 22** destructive-capable reconcilers with **zero** declared exceptions. No workflow invokes this script directly, and its pre-existing Apply-mode `-WhatIf` short-circuit still returns before Phase 1 ever runs (unchanged, out of scope) — so `drift-detection.yml`'s `Deploy-*.ps1` glob dispatch, which does reach this script by name, never exercises the new gate in CI; `-IsWhatIf` is still bound correctly to `$WhatIfPreference` regardless, since the AST contract checks the binding exists independent of reachability. Full repo Pester suite green (2020 tests, 0 failed); PSScriptAnalyzer clean on both changed files. Does **not** touch [#106](https://github.com/marcusjacobson/Purview-as-Code/issues/106) (the UnifiedCatalog audit-delete bug, unrelated, still open). References [ADR 0052](docs/adr/0052-destructive-confirmation-gate-at-script-layer.md) and [ADR 0053](docs/adr/0053-overwrite-foreign-author-switch.md) (#105)
- **scripts:** gate `Set-AuditRetentionPolicy.ps1` per ADR 0052 and widen the rollout's own completion guard from a `Deploy-*.ps1` filename glob to a destructive-capability predicate. **This is the 22nd reconciler, and #83's own batch-3 agent found it: it is `Set-*`, not `Deploy-*`, so it was invisible to every count in #83** — the "21 reconcilers" figure, `tests/scripts/ConfirmGate.Tests.ps1`'s self-verifying class map, and the AST-contract `-ForEach` list were all `Deploy-*.ps1` globs, and all three were blind to it by construction. It carried the **original issue #85 defect, still live**: `ConfirmImpact = 'Medium'` at `ConfirmImpact >= $ConfirmPreference` (default `High`) means `Medium < High`, so every `$PSCmdlet.ShouldProcess(...)` returned `$true` without ever prompting. It declares `-PruneMissing` and calls `Remove-UnifiedAuditLogRetentionPolicy` with **no confirmation at any layer** — and **no workflow invokes this script**, verified by grep across `.github/workflows/**`, so there was no CI-layer gate behind it either; its own help text falsely claimed the confirmation was "a CI-layer concern enforced by the workflow." **The durable fix is the widened population, not just the one script**: `$script:AllReconcilersOnDisk` (the completion criterion), the destructive-branch class map's self-check, `Get-GatedReconciler` (which also drives the CI-hang scanner's gated set), and the AST-contract's `-ForEach` derivation all moved from `Get-ChildItem -Filter 'Deploy-*.ps1'` to a predicate — every script under `scripts/` that declares `[switch]$PruneMissing` — verified against the whole directory (64 files, zero parse errors) to match exactly the 21 `Deploy-*.ps1` reconcilers plus this one. **Two of those four call sites had a second, independent bug the rollout brief didn't anticipate**: `Get-GatedReconciler` and the AST-contract's `-ForEach` list *also* pre-filtered their enumeration to `Deploy-*.ps1` before ever checking "does this script carry a gate call" — so even after gating the script, neither would ever have examined it, and the completion criterion could not have gone green no matter what the script contained. Both were widened to scan every `*.ps1`, leaving the (already filename-agnostic) "has a gate call" predicate as the sole determinant of membership. **Red-then-green, in two commits**: with the population widened and the script still ungated, the completion criterion and the CI-hang scanner's `Should -Be 21` assertion both went RED, by name, citing `Set-AuditRetentionPolicy.ps1` as the missing script (captured verbatim in the PR); gating the script in the second commit turned both green with no other change. The gate itself copies the `Deploy-Labels.ps1` / `Deploy-DLPPolicies.ps1` reference shape exactly: `ConfirmImpact` raised to `'High'`, `ConfirmGate.psm1` imported alongside the existing `DirectionPolicy.psm1`, both destructive branches gated via `$PSCmdlet.ShouldContinue()` and keyed on the plan (`$repoWinsOverwrites`, populated in the one place the repo-wins decision is made; the prune target list, derived from the plan's `Orphan` rows one line above the gate) — never on `$DirectionPolicy` — with a decline throwing the exact contract phrase `No tenant writes were made`. `[switch]$Force` was already declared, and the audit-mode `$WhatIfPreference = $true` short-circuit (the same safe mechanism as Collections/DataSources/DLPPolicies, not the broken UnifiedCatalog pattern from #106) needed no change. Verified by driving the **real, AST-extracted gate block** from the committed script (not a re-implementation) against synthetic plans: the overwrite gate prompts on a non-empty repo-wins plan naming the object and count, and stays silent when the list is empty; the prune gate prompts on a non-empty orphan plan under `-PruneMissing`, and stays silent without it. A B2 mutation (`if ($DirectionPolicy -eq 'repo-wins') { $repoWinsOverwrites.Add(...) }`, replacing the plan-keyed unconditional `.Add()`) was injected and confirmed to flip rule (b) red, by name, then reverted. Full repo Pester suite green (1985 passed, 0 failed); PSScriptAnalyzer clean on both changed files. Does **not** touch [#105](https://github.com/marcusjacobson/Purview-as-Code/issues/105) (`Deploy-EntraDirectoryRoles.ps1`, still the one declared exception) or [#106](https://github.com/marcusjacobson/Purview-as-Code/issues/106) (the UnifiedCatalog audit-delete bug, unrelated, still open). References [ADR 0052](docs/adr/0052-destructive-confirmation-gate-at-script-layer.md) and [ADR 0053](docs/adr/0053-overwrite-foreign-author-switch.md) (#108, refs #83, #85, #105)
- **ci:** **complete the [#83](https://github.com/marcusjacobson/Purview-as-Code/issues/83) rollout (PR-B, final batch)** — derive the ADR 0052 **CI-hang guard** from the AST, and bind `-Confirm:$false` on the **three** CI invocation sites that bound no suppressor at all. **20 of 21 reconcilers are now gated**; `Deploy-EntraDirectoryRoles.ps1` is the one **declared** exception ([#105](https://github.com/marcusjacobson/Purview-as-Code/issues/105) — its interleaved single-pass loop POSTs creates *before* the revoke plan exists, so the gate's mandated `No tenant writes were made` abort message would be **a lie**; the guard asserts it currently has **zero** gates, so gating it without deleting the carve-out goes **RED** — the exception cannot go stale). **The guard this replaces was vacuous, and it is the third instance of the same green-by-absence defect in this one issue.** Its `Context ... -ForEach` was a **hardcoded list of four workflows** checking invocations of **two** scripts (`Deploy-Labels`, `Deploy-DLPPolicies`). Twenty scripts are gated. **Eighteen were unexamined.** The rewrite derives the gated set from the same AST predicate the gate contract uses, extracts **every** `run:` block from **every** workflow (150 blocks; 37 mention a reconciler; **0** fail to parse), parses each as **PowerShell** rather than regex, and finds **29 invocation sites** — and it states all four counts and **fails if any is zero**, because *a scanner that matches nothing passes green and asserts nothing*. Parsing rather than regexing is what lets `validate.yml`'s `$exempt = @('Deploy-Classifications.ps1', …)` be recognised for what it is — **string literals consumed by `Get-Content`, not invocations** — and left unflagged, so the guard does not become a nuisance the next author silences. It also matches the shape most likely to be silently skipped: `drift-detection.yml:113` dispatches **all twenty** gated scripts weekly on a hosted runner via `& $script.FullName -WhatIf` over a `Get-ChildItem` glob — an **expression**, not a literal path, so a scanner looking only for literal script names finds **zero** invocations there and passes. It is safe (`-WhatIf` short-circuits the gate before the prompt), but it is now **proven** safe rather than assumed, with its own non-vacuity floor. **⚠️ The rollout brief's suppressor list was wrong, and the guard deliberately contradicts it: `-Force` is NOT a suppressor.** `-Force` is a *custom* switch — it silences `ConfirmGate` (which honours it) and does **nothing whatever** to `$PSCmdlet.ShouldProcess`, which consults only `$ConfirmPreference`; [ADR 0053](docs/adr/0053-overwrite-foreign-author-switch.md) §4 deliberately **removed** the `if ($Force) { $ConfirmPreference = 'None' }` ambient self-disarm that used to paper over the difference. Verified by experiment, not read off the docs: a probe at `ConfirmImpact = 'High'` invoked as `pwsh -c "& ./probe.ps1 -Force"` with stdin closed does **not** proceed — `ShouldProcess` **throws** (*"PowerShell is in NonInteractive mode. Read and Prompt functionality is not available."*); the same probe with `-Confirm:$false` or `-WhatIf` runs clean. **No site relies on `-Force` alone today**, so excluding it changes no verdict now — it is excluded so the first author who reaches for `-Force` (the natural mistake, since `-Force` *is* what silences the gate by hand) gets a red test instead of a hung weekly job. The **three naked sites**: `sync-irm-from-tenant.yml:206` (`Deploy-IRMPolicies.ps1 -DirectionPolicy audit`) does not hang today **only** because that script's audit short-circuit **does not return** — it sets `$WhatIfPreference = $true` and **falls through into the gate and apply region**, so the gate *is* reached and stays silent purely because its `-IsWhatIf` argument happens to be bound to `$WhatIfPreference`. **That is a load-bearing coincidence, not a guarantee** — [#106](https://github.com/marcusjacobson/Purview-as-Code/issues/106) is a live bug of exactly the class where an audit short-circuit fails to neutralize the writes. `deploy-label-policies.yml:372` and `deploy-auto-label-policies.yml:390` (`-VerifyPublished`) are bound as **defence in depth, and said to be**: `-VerifyPublished` **provably cannot** reach a prompt today — the `if ($mode -eq 'Verify')` block ends in an **unconditional `return`**, and all 13 / 10 `ShouldProcess` sites and both gate sites in each script lie **strictly after it** — but the binding costs nothing and removes the dependence on that proof staying true. **Verified by red-replay, which is the only proof that matters:** strip `-Confirm:$false` from `sync-label-policies-from-tenant.yml` (leaving `-Force`) and the **old guard stays GREEN — 8 passed, 0 failed**, because it never scanned that file, while the new one goes **RED**, naming file, line and command; strip `-WhatIf` from the drift-detection glob loop and the old guard is again **GREEN** while the new one flags `drift-detection.yml:96 [glob-dispatch]`; strip `Confirm = $false` from `deploy-labels.yml`'s repo-wins splat — a mutant the old guard *did* cover — and **both** go red, proving the new guard is a strict **superset** that lost no coverage. Also corrects **stale help text in 13 files, not the 5 previously reported**: twelve gated reconcilers still told operators that the overwrite confirmation "*is a CI-layer concern … local script callers are operator-trusted*" — **now false**, and dangerously so, since per [ADR 0045](docs/adr/0045-template-kickoff-spinoff-model.md) a pre-kickoff clone of this template **has no working CI at all**, making the script-layer gate the **only** defence it has. `Deploy-Classifications.ps1:130` documented a `-DirectionPolicy repo-wins` parameter **it does not declare** (it is Class B). Full suite **1988 tests, 0 failures**; PSScriptAnalyzer clean across 14 changed files; `yamllint` clean on the changed workflows. **The honest boundary, stated rather than sold:** the gate is **LATENT under `portal-wins`** today — all 11 Class A scripts pass `-HasDrift $true`, so `portal-wins` skips drifted rows and the overwrite list is empty. Plan-keying's value is that it **survives the next `HasDrift` bug**, which is exactly what F4 was; it is **not** catching a live bug, and must not be sold as one. **B1** — policy laundered through a local variable — **remains uncaught** and is documented as such in the suite. (#83)
- **scripts:** key the ADR 0052 destructive-confirmation gate on the **plan**, not on `-DirectionPolicy`, and fix the `Deploy-UnifiedCatalogPolicies.ps1` direction-policy bug that proved why it matters. **This is PR-A of [#83](https://github.com/marcusjacobson/Purview-as-Code/issues/83); the 17-reconciler rollout follows in PR-B, replicating the pattern this PR corrects.** The three ADR 0052 reference implementations gated the overwrite branch on a **conjunction** — `if ($DirectionPolicy -eq 'repo-wins' -and $overwrites.Count -gt 0)`. That policy conjunct is **either redundant or dangerous, and never useful**: redundant wherever `portal-wins` genuinely skips drifted objects (the overwrite list can then only populate under `repo-wins` anyway), and dangerous wherever it does not. `Deploy-UnifiedCatalogPolicies.ps1` was exactly that "wherever": it looped over the **whole** plan and passed a hardcoded `-HasDrift $false` into `Resolve-DirectionPolicyAction`, which only skips when `$HasDrift -and $Policy -eq 'portal-wins'` — so **`portal-wins` never skipped**, `repo-wins` and `portal-wins` were behaviourally identical, and **every drifted role assignment was overwritten regardless of policy**. Replicating the reference keying into it would have shipped a gate that populates its overwrite list, evaluates the policy conjunct **false**, **never fires**, and lets a **permissions surface** be overwritten with no confirmation. *A guard that can pass vacuously is worse than no guard, because it is believed.* **The rule, now stated in `ConfirmGate.psm1` and enforced by test: key the gate on the PLAN — the set of objects the run will actually destroy — never on the policy.** This is the same principle ADR 0052 settled when it chose `ShouldContinue` over `ShouldProcess`, and [ADR 0053](docs/adr/0053-overwrite-foreign-author-switch.md) when it made `Test-ConflictRow` pure: **the guard must not depend on a negotiable proxy.** `$DirectionPolicy` is a proxy for "will this overwrite?", and a fallible one; the plan is ground truth. Proven non-vacuously against pre-fix `1d4f855` by AST-extracting the *real* `Invoke-DirectionPolicyPlan` and driving the *real* `ConfirmGate`: under `portal-wins` with two objects the run **will** overwrite, policy-keying raises **0 prompts** and plan-keying raises **1**. ⚠️ **OPERATOR-VISIBLE BEHAVIOUR CHANGE on the DEFAULT policy, on a permissions surface.** The `-HasDrift` fix is **two parts, and needs both**: filter to `Action -eq 'Update'` (drift is the only thing the direction policy arbitrates) **and then** pass `-HasDrift $true` — mirroring `Deploy-UnifiedCatalog.ps1`, which has had the filter all along. Passing `-HasDrift $true` **alone**, without the filter, makes `portal-wins` skip every **create** and every **prune** as well, turning the reconciler into a **silent no-op on its default policy** — strictly worse than the bug it fixes; that regression is pinned by name in the tests. After this change, a `portal-wins` run of `Deploy-UnifiedCatalogPolicies.ps1` **stops overwriting drifted Unified Catalog role assignments that it overwrites today** and reports them as `Skip` instead. That is the correct behaviour per [ADR 0029](docs/adr/0029-source-of-truth-direction-policy.md) — `portal-wins` exists to preserve tenant edits — but an operator who has been relying on the buggy behaviour to reconcile role assignments under the default policy must now pass `-DirectionPolicy repo-wins` explicitly. `Deploy-UnifiedCatalogPolicies.ps1` also gains the two ADR 0052 gates (it was already `ConfirmImpact = 'High'`; it had **no** gate), and `ConfirmGate.psm1`'s help — which still asserted ADR 0052's **false** premise that the foreign-author override "was never implemented in any of the 21 reconcilers" — is corrected to point at ADR 0053, which proved it was implemented in **six**. One further defect is recorded rather than quietly fixed, because it is the same failure mode one layer up: the first cut of the new source-text assertion used `Should -Match 'This run will OVERWRITE'`, which is **case-insensitive**, and it passed against pre-fix `Deploy-Labels.ps1` — a script with **no plan-keyed gate at all** — by matching the lowercase **comment** on line 1804. **An assertion that can be satisfied by a comment is not an assertion about the code.** It is now `-CMatch` and anchored to the query assignment, so prose is structurally incapable of satisfying it. (#83)
- **data-plane:** empty **every root list** under `data-plane/**` and ship [ADR 0056](docs/adr/0056-template-ships-empty-desired-state.md), which **amends [ADR 0046](docs/adr/0046-tenant-placeholder-manifest.md) again** and **supersedes one claim of [ADR 0055](docs/adr/0055-identifier-shaped-residual-scan.md)**. This repository is a **public template**. A consumer who clicked *Use this template*, ran kickoff exactly as designed, and dispatched the deploy workflows got **the owner's live lab governance objects created in their own tenant** — including `Lab-AutoLabel-SSN` at `mode: Enable` / `exchangeLocation: [All]`: a **live, enforcing, tenant-wide policy that stamps an encrypted label on any mail containing a U.S. Social Security Number, across every mailbox**. The policy immediately above it in the same file was at `TestWithoutNotifications`, so **simulation mode was known, available, and not used** — this was a per-file judgment call, not a tooling gap, and that is what determines the fix. `data-plane/**` was the owner's real lab, string-substituted: the surviving `c8iacz` / `chfb1r` collection names are **Purview's auto-generated IDs** (nobody hand-authors those), and `Scan-DataLakeModerninzation` is **a typo** — the tell that proves provenance more decisively than any GUID. **The rule is now uniform and mechanical: the template ships nothing deployable.** Eleven files are emptied (`collections`, `data-sources`, `scans`, `dlp`, `labels`, `label-policies`, `auto-label-policies`, `irm`, `glossary`, `classifications`, `adaptive-scopes`), joining the 15 already empty — **including the three that disclosed nothing**, because *a per-file "is this one safe enough to ship?" call is exactly how the SSN policy shipped*, and a uniform rule cannot be eroded by judgment. **Only an EMPTY ROOT LIST is a true no-op** (#96's shape decision, generalised): a populated list is deployable desired state, and `members: []` on a *listed* object means "revoke everything". Three carve-outs, each **named, reasoned, and pinned by the test**: `sit-catalog.yaml` **stays populated** (Microsoft built-in reference data, no reconciler — and `Deploy-Labels.ps1:1262` **errors** if a label's `autoApplicationOf` sitId is missing from it, so emptying it would *break* label deploys); `seed-skip-names.yaml` **stays populated** (a *skip* list with **inverted polarity** — emptying it adds 31 undeletable Microsoft seed objects back into every `-PruneMissing` run, making things **less** safe); and the ADR 0017 label fixture, which no reconciler reads. The DSPM config mappings are **ruled explicitly IN scope and OUT of the list rule**, with the ruling *checked* rather than left vacuously true. `collections.yaml`'s `rootCollection` scalar (the Purview **account name**) ships unset. (#100)
- **data-plane:** drop the four `:!data-plane/...` `intentionalSamples` exclusions from `.github/agents/tenant-placeholders.yaml`. They asserted those files were "sample source names" / "sample scan targets". **They were not samples** — they were the four carrying a real Databricks workspace, a real Dataverse org, a real Entra group named as a label rights holder, and Purview's auto-generated collection IDs. The comment described why each line was *added* and not what it *did*, which is the identical failure ADR 0055 diagnosed one layer up and then **reproduced inside its own fix**. Removed rather than re-scoped: with the root lists empty there is no sample content left in the tree to exclude. (#100)

### Added

- **examples:** add [`examples/`](examples/README.md) — the worked, **fully scrubbed**, non-deployable reference content, in a tree **no workflow, script, or schema reads** (asserted by the guard test). In-file comments were rejected: *a commented-out enforcing SSN policy still sits in the exact file a reconciler opens — the fix is a `#`, and the regression is deleting one character.* **Moving content RELOCATES a disclosure; it does not remove one**, and this repo is public — so every example was scrubbed to synthetic values (auto-generated collection IDs → readable slugs; real Databricks / SQL-warehouse / Dataverse endpoints → zero-filled synthetics; the five `user@contoso.com` rights holders that were a **genericization collapse of five real principals** → five distinct synthetic groups). **`examples/**` is deliberately IN SCOPE for both residual scans** — no `:!examples` exclusion, because *a path exclusion is the mechanism that hid the last disclosure*; verified non-vacuously (the identifier scan reports 14 GUID occurrences there, all acquitted by `catalogKeys`). **Anything whose only value was as a lab artifact is DELETED, not relocated** — the `Pilot — … (Lab)` teardown fixtures, the duplicate scans, the typo'd scan, and **`Lab-AutoLabel-SSN`, which does not survive as an example in any form.** (#100)
- **tests:** generalise `tests/data-plane/ShippedDesiredState.Tests.ps1` from **two hard-coded files to every root list in the tree**, with the three carve-outs named, reasoned, pinned, and **verified against the source** (it greps `scripts/` to confirm no SIT writer exists and nothing reads the fixture, rather than trusting the ADR). **This is the control that makes "clean" a build check instead of a claim** — and it is verified against the broken state, because *a guard that passes on the broken state is worthless*: run against `main` (`9bea348`) it **FAILS**, naming all 11 files and all 14 populated root lists. It also blocks the **shape** rather than the name — no auto-label policy in `data-plane/**` or `examples/**` may ship at `mode: Enable`, because a name blocklist only catches the instance you already found. Adds `tests/data-plane/ShippedDesiredState.NoOp.Tests.ps1`, which **proves the deploy is now a no-op AND that the fix was needed**, contacting no tenant: it drives the reconcilers' own desired-state builders (lifted by AST) with the shipped YAML and gets **zero** planned operations, then with a populated file and gets **creates**. *A test that only proved the first would also pass against a reconciler that does nothing at all.* (#100)

- **scripts:** add `scripts/Test-IdentifierResidue.ps1` — an **identifier-shaped, fail-closed residual scan** — and [ADR 0055](docs/adr/0055-identifier-shaped-residual-scan.md), which **amends [ADR 0046](docs/adr/0046-tenant-placeholder-manifest.md) by supersession**. ADR 0046 claimed that "scanning everything *except* these paths means any remaining match is a genuine missed tenant surface." **That claim was unsound**, and it failed twice over, either failure fatal alone: (1) `intentionalSamples` carried a blanket `:!data-plane` exclusion — written for sample catalog content, it silently excluded **the entire tree that holds the desired state**; and (2) `residualScan.pattern` (`contoso|onmicrosoft\.com|OWNER-PLACEHOLDER`) is **token-shaped**, so it could not have matched a raw GUID even with the exclusion removed. **A token-shaped scan cannot verify the absence of identifier-shaped data.** The provenance is the durable lesson: `data-plane/**` is the owner's real lab, string-substituted — genericization replaced `contoso` **tokens**, and **GUIDs are not tokens**, so they survived the substitution *and* the verification, landing in the one directory the scan ignored. A verification that shares its model of the world with the transform it verifies is not a verification. The new scan walks **every tracked file with no path exclusions of any kind** (a path exclusion is what caused the disclosure) and treats every non-zero GUID as a finding unless one of four manifest rules acquits it: `syntheticShapes` (by **shape** — the zero GUID, the reserved fixture namespace `00000000-…-<counter>`, repeated-nibble fixtures; this is what replaces a `tests/**` path exclusion, so a fixture is acquitted for *looking* synthetic, never for *living* in `tests/`), `catalogKeys` (by **(file, key)** — ~330 built-in SIT / rule-pack / trainable-classifier IDs, chosen over a 330-line value list precisely because *a reviewer in the habit of rubber-stamping allow-list additions is how a real object ID gets waved through*), `microsoftConstants` (by **exact value**, 10 named + cited Entra role `templateId`s, Azure RBAC role-definition IDs, and Microsoft first-party app IDs), and `reviewRequired` (a SHA-256-keyed **quarantine**, pinned by a Pester test so it cannot grow silently). Verified against the pre-scrub commit with today's manifest: the scan **fails and flags exactly the 15 disclosed object IDs**, while correctly acquitting the three Microsoft role `templateId`s in the same file — a scan that cannot catch the leak it was built for is theatre. Zero false positives against `main` (~385 legitimate GUIDs, all acquitted). Wired into `validate.yml` so it runs on **every PR**, not only at onboarding — an onboarding-only scan cannot catch a regression, and the disclosure proves it: the identifiers entered via a scaffold commit and nothing looked at them again. This finally puts a machine behind `copilot-instructions.md` line 150, which has specified this scan **as a literal grep command** all along and which nothing has ever run, and enforces [ADR 0023](docs/adr/0023-identifier-resolution.md) Category 3 (principals are named by `displayName`, never a raw object ID) for the first time since it was accepted. (#94)
- **tests:** add `tests/data-plane/ShippedDesiredState.Tests.ps1` — **the first test in this repository that reads a shipped `data-plane/**` YAML.** It asserts the privileged root lists (`roleGroups`, `directoryRoles`) ship **empty** in the template (not merely a privacy default — a **safety** default: *not listed* is the only shape that means *not managed*, since a role group **listed** with `members: []` means "this role group must have zero members" and revokes every existing assignment under `-PruneMissing`), and that **no raw GUID appears under any principal key in any shipped data-plane YAML, in any copy, forever** ([ADR 0023](docs/adr/0023-identifier-resolution.md) Category 3). More *reconciler* coverage would **not** have caught the disclosure: `Deploy-PurviewRoleGroups.Tests.ps1` already pins revoke-everything-on-empty-members across ~20 `It` blocks, and its YAML shipped ~50 `members: []` rows and 15 real object IDs regardless. **The hazard was in the DATA, so the test must read the DATA** — and until now, zero tests loaded the shipped YAMLs at all. `tests/Run-Pester.ps1` is widened from `tests/scripts` to `tests/` so the new directory is actually discovered. (#94)

### Changed

- **adr:** add [ADR 0052](docs/adr/0052-destructive-confirmation-gate-at-script-layer.md) — the destructive-operation confirmation gate belongs at the **script layer**, via `ShouldContinue` — and land the reference implementation on `Deploy-Labels.ps1`, `Deploy-FilePlan.ps1`, and `Deploy-DLPPolicies.ps1`. This closes a **live violation of a written contract**, not a missing feature: `powershell.instructions.md` has mandated a delete-confirmation prompt since the reconcilers first shipped, and **no such prompt has ever been emitted**. Root cause: 20 of the 21 `Deploy-*.ps1` declared `ConfirmImpact = 'Medium'`, PowerShell only prompts when `ConfirmImpact >= $ConfirmPreference`, and `$ConfirmPreference` defaults to `High` — so `Medium < High` meant **every `$PSCmdlet.ShouldProcess(...)` call returned `$true` without ever prompting**. `-WhatIf` worked perfectly throughout, which is exactly why it survived. (The on-disk spelling has spaces around the `=`, so `grep "ConfirmImpact='Medium'"` returns zero matches and a false sense of safety.) The fix is a change of **method, not of constant**: raising the impact level alone would leave the gate hostage to a caller-controlled `$ConfirmPreference`, so both destructive branches — the `-PruneMissing` **delete** and the `-DirectionPolicy repo-wins` **overwrite** — are now gated by `$PSCmdlet.ShouldContinue()` via the new shared `scripts/modules/ConfirmGate.psm1`, which performs **no** impact/preference comparison and therefore cannot be silently defeated. One prompt per run (the `yesToAll`/`noToAll` overload, with the `[ref]` pair shared across both gates — never one prompt per object), suppressed by `-Force` or an explicit `-Confirm:$false`, and short-circuited **before** the prompt under `-WhatIf` so a dry run still previews the deletes it would make. ADR 0029's workflow-layer typed-token gate is **retained** as defence-in-depth: it was never wrong, merely insufficient — its §Consequences claim of "no local-script escape hatch" was false the day it was written, because the local script **is** the escape hatch and always was, the gate having been layered where the local path does not traverse. `Deploy-FilePlan.ps1` proves the point: it has **no workflow caller at all**, so the local terminal is the only path it has. Also settles the `-Force` overload, which on inspection was a **three-way** collision, not a two-way: the contract's "override a foreign `lastModifiedBy`" meaning is **retired rather than reassigned**, because the IPPS surface returns no `lastModifiedBy` to diff against and no reconciler ever implemented it (`Deploy-Labels.ps1`'s own header has said so all along), leaving `-Force` with the single coherent meaning it already had in code — *suppress the safety guard on the operation you asked for* — and the `Conflict` drift-report row marked **reserved, not currently emitted**. Two further findings came out of verifying (rather than assuming) the issue's premise that no workflow changes were needed, and **it was wrong on both counts**. First, `Deploy-Labels.ps1` wraps its `-ExportCurrentState` YAML write in `ShouldProcess`, and two workflows (`deploy-labels.yml`, `sync-labels-from-tenant.yml`) invoked that export path **without** `-Confirm:$false` — at `ConfirmImpact = 'High'` those steps would have raised a prompt no one can answer on a hosted runner and **hung the job**, converting a silent safety defect into a CI outage; all four export call sites now bind `-Confirm:$false`, and a Pester guard asserts it (verified non-vacuous: replayed against the pre-fix workflows it flags exactly those four). Second, it repairs [ADR 0035](docs/adr/0035-records-seed-content-immovable.md), which **we** broke in #82: its 31-name records seed-skip baseline was anchored to the `skip_names_records` input of `deploy-data-plane.yml`, a workflow ADR 0051 **deleted**, leaving a mandated safety baseline living **nowhere executable**. It is relocated to the checked-in data file `data-plane/records/seed-skip-names.yaml`, which `Deploy-FilePlan.ps1` now unions into its effective skip list on **every** run — including the local run that is the only run it has — so operators may *extend* the list but can no longer *shrink* it from the command line, enforcing ADR 0035 Decision #3 mechanically rather than by convention. ADR 0035 gets a dated Update note and corrected pointers; its decision is **not** rewritten. The records risk is stated precisely rather than inflated: the 31 seeds are **undeletable** (every `Remove-FilePlanProperty*` fails with `ErrorRuleNotFoundException`), so a prune without the skip list yields **31 noisy failures, not 31 deletions** — the real data-loss exposure the gate addresses is to **operator-authored** objects, which delete just fine. Rollout to the remaining 18 reconcilers (#83) and the repo-wide guard test (#84) stay open, in that order (#85)
- **repo:** remove the phantom `Purview-as-Code-Generic` sibling folder from `Purview-as-Code.code-workspace`, so the multi-root workspace declares only `{ "path": "." }` — the second entry pointed at a directory that exists on no clone, shipped broken to every "Use this template" consumer (ADR 0045), and was silently auto-removed by VS Code / Cursor on open, re-dirtying the working tree and tripping the clean-tree precondition of the agent-led lifecycle on every item; the dangling `"powershell.cwd": "Purview-as-Code-Generic"` setting is deleted with it (owner ruling on the issue's open question), since PowerShell already defaults to the sole remaining workspace folder and hardcoding a folder *name* in a template reproduces the same defect class for any consumer who renames the repo folder, leaving `"settings": {}` — the shape VS Code itself emits (#71)

### Fixed

- **role-groups:** remove real tenant identifiers from the committed desired-state lists in `data-plane/entra-directory-roles/role-assignments.yaml` and `data-plane/purview-role-groups/role-groups.yaml`, and ship both root lists empty (`directoryRoles: []` / `roleGroups: []`). This is a **pure data change** — no reconciler, schema, or test behaviour is touched. A public template repository was disclosing a set of live Microsoft Entra security-group object IDs in *active* desired-state rows (not comments, not examples), each annotated with a comment naming the group's purpose — which materially worsened the disclosure, since the annotations mapped privileged group identities to the tenant-wide directory roles they hold. Object IDs are identifiers, not credentials, so **there is nothing to rotate**; but they are reconnaissance-grade tenant topology, and both `.github/copilot-instructions.md` (Identifier rules) and [ADR 0023](docs/adr/0023-identifier-resolution.md) had prohibited committing them since before these files were written. The same commit drops the per-row export-provenance strings and the per-row `Current tenant assignments: N user(s)` counts, which disclosed which role groups are populated and how many administrators each carries. **The shape of the fix is the entire substance of it, and both obvious scrubs are catastrophic.** These are desired-state lists consumed by reconcilers that support `-PruneMissing`, so setting `members: []` on a row that stays *listed* does not mean "no members declared" — it means "this role must have **zero** members", and `Deploy-EntraDirectoryRoles.ps1:875` / `Deploy-PurviewRoleGroups.ps1:699` compute `$toRevoke` as *tenant-minus-desired*, so an empty desired set revokes **every existing assignment**. The zero-GUID placeholder is **not** a guard against this and is the subtler trap: there is no zero-GUID special-case anywhere in either reconciler, so on `Deploy-EntraDirectoryRoles.ps1` the placeholder is sent to the `GET /groups/{id}` role-assignable probe, **404s**, is recorded as a `Conflict` row and `continue`d past (L838-846) — silently dropping it from the validated member set, leaving `$desiredSet` empty, and landing in the *identical* revoke-everything state; on `Deploy-PurviewRoleGroups.ps1`, which has no probe at all, it is instead handed straight to `Add-RoleGroupMember` and fails at the Exchange cmdlet while the real tenant members still land in the revoke set. The only safe shape is to empty the **root** list rather than the `members:` lists, because both reconcilers hard-code a pre-flight no-op that returns before reading or writing any tenant state when the root list is empty (`Deploy-EntraDirectoryRoles.ps1:789`, `Deploy-PurviewRoleGroups.ps1:647`) — **"not listed" is the only shape that means "not managed"**. Both file headers had already declared the empty root list the intended default steady state all along; the exported rows simply never got scrubbed. Verified by driving both reconcilers against a synthetic non-empty tenant with the network boundary stubbed: the shipped shape performs **zero** tenant reads and **zero** revokes, while the two rejected shapes each compute a non-empty `$toRevoke` and execute revocations — the no-op assertion is red against both, so it is not vacuous. The three built-in directory-role `templateId` values are **preserved**: they are Microsoft-published, tenant-independent role-template constants, not tenant identifiers, and removing them would break the file. Microsoft built-in sensitive-information-type and trainable-classifier GUIDs elsewhere under `data-plane/` are likewise untouched — they are explicitly exempted by `.github/instructions/sample-data.instructions.md` and are not a disclosure. Both files gain an explicit warning against the two unsafe shapes so the next operator does not reintroduce them, and the field documentation that previously described `members: []` as a benign default is corrected. Scrubbing `HEAD` stops the *ongoing* disclosure; it does not un-disclose — the repository has been public, so the identifiers should be assumed cached, forked, or crawled, and the owner should review PIM activation, eligibility, approval requirements, and Conditional Access coverage on the affected groups. Git-history purge, a residual-identifier scanner, and migration to the ADR 0023 displayName resolution shape are tracked separately and deliberately do **not** gate this scrub (#92)
- **scripts:** split the foreign-author overwrite override out of `-Force` into its own switch, `-OverwriteForeignAuthor`, and land [ADR 0053](docs/adr/0053-overwrite-foreign-author-switch.md) to amend [ADR 0052](docs/adr/0052-destructive-confirmation-gate-at-script-layer.md) §6 by supersession. **ADR 0052's premise was false.** It retired `-Force`'s third meaning — "overwrite objects whose `lastModifiedBy` is not the deploy principal" — as *unimplementable*, on the claim that "zero reconcilers diff a `lastModifiedBy`, because the IPPS / Security & Compliance cmdlets do not return one". **Six reconcilers diff one today** and emit real `Conflict` rows from a real authorship field: `Deploy-Glossary.ps1` and `Deploy-Classifications.ps1` (`updatedBy` / `createdBy`, Atlas v2), `Deploy-DataSources.ps1` and `Deploy-Scans.ps1` (`lastModifiedBy` … `systemData.lastModifiedBy`, Scanning REST), and `Deploy-UnifiedCatalog.ps1` / `Deploy-UnifiedCatalogPolicies.ps1` (`systemData.lastModifiedBy` / `LastModifiedBy`, Unified Catalog preview REST). **How it got it wrong is the durable lesson, and it is recorded as such:** the grep *did* hit — `Deploy-DataSources.ps1` carries a literal `lastModifiedBy` — but ADR 0052 read the three **comment** lines in its own grep output that say the capability is impossible (`Deploy-Labels.ps1:33`, `Deploy-LabelPolicies.ps1:42`, `Deploy-PurviewRoleGroups.ps1:45`) and generalised from them **without reading the code lines in the same output**; those three comments are true *and scoped to their own surface*. It then generalised from an unrepresentative sample — all three of its reference reconcilers sit on the **IPPS / Security & Compliance** cmdlet surface, which genuinely returns no authorship field, while the **Atlas / Data Map / Unified Catalog REST** surface does. The rule this repo now carries: **a capability claim of the form "no reconciler can X" is only sound if it was tested against a reconciler from *each* of the two authoring surfaces.** The override is un-retired under the dedicated switch ADR 0052 line 120 itself pre-authorised (`-OverwriteForeignAuthor`), added to the **Apply parameter set only** of the six, so `-Force` keeps exactly **one** meaning — *suppress the safety guard on the operation you asked for* — and no longer suppresses `Conflict` rows. The six were broken by **two distinct mechanisms needing two distinct fixes**, which is why a single blanket rebind would have papered over the more serious one: under **Mechanism A** (`Glossary`, `DataSources`, `Classifications`, `Scans`) `Test-ConflictRow` opened with `if ($ForceEnabled) { return $false }`, so `-Force` **suppressed the Conflict classification at source** — the row was never emitted, fell through to `Update`, and the portal-authored object was **silently overwritten with no record in the drift report**; under **Mechanism B** (`UnifiedCatalog`, `UnifiedCatalogPolicies`) the row *was* emitted and the overwrite proceeded anyway, via `-AllowConflictOverwrite:$Force.IsPresent` at the call site. Mechanism A destroyed the audit record, not merely the object. Both are fixed, and the invariant now holds structurally: **a write over a foreign-authored object is ALWAYS reported as a `Conflict` row, never laundered into a plain `Update`** — the switch grants **permission, not silence**; a safety override that erases the evidence it fired is a laundering step, not an override. **The obvious fix for Mechanism A is wrong, and this PR shipped it before review caught it — the near-miss is recorded in ADR 0053 §Alternatives-5 rather than quietly repaired.** Rebinding `Test-ConflictRow`'s `-ForceEnabled` parameter to `-OverwriteForeignAuthor` *preserves* the suppress-at-source short-circuit (`if (...) { return $false }`): under the new switch the predicate returns false, the plan falls through to `Update`, and the object is overwritten **while the `Conflict` row vanishes** — the exact alternative the ADR rejects by name, passing a suite that only ever asserted the *no-override* path. The structural fix: `Test-ConflictRow` is now a **pure authorship predicate** taking `(TenantRaw, DeployIdentity)` and **no override input at all**, so it is *incapable* of suppressing its own finding; a new pure `Resolve-ConflictPlanAction` owns the override decision and returns `Category = 'Conflict'` in **both** branches, varying only whether the plan `Action` is a write; and the apply loop derives the report category from the row's `Conflict` flag, so a conflicted `Update` reports as `Conflict`. This mirrors Mechanism B's `Get-ReconciliationPlan`, which had the shape right from the start. The generalisable rule: **a predicate that can be told to return `$false` is not a predicate, it is a switch with extra steps.** Note the ergonomic cost, stated rather than glossed: `-OverwriteForeignAuthor` and `-DirectionPolicy` are **independent axes and both must permit the write**, so overwriting a foreign-authored object requires `-DirectionPolicy repo-wins -OverwriteForeignAuthor` — under the default `portal-wins` the object is skipped whatever its authorship and the switch alone is inert. Also **deletes the two ambient `if ($Force.IsPresent) { $ConfirmPreference = 'None' }` self-disarms** (`Deploy-UnifiedCatalog.ps1`, `Deploy-UnifiedCatalogPolicies.ps1`), which ADR 0052 line 89 explicitly forbids while two scripts in its own blast radius did exactly that **to themselves** — and `Deploy-UnifiedCatalogPolicies.ps1` is the **one reconciler already at `ConfirmImpact = 'High'`**, so that line was precisely what had been neutering the only script that looked correct; **this is a real behaviour change for it**, its per-write `ShouldProcess` calls now being live under `-Force` (workflow callers are unaffected — they bind `-Confirm:$false` — but a local `-Force` run now prompts). `Deploy-AdministrativeUnits.ps1` is the **counter-example that makes the rule legible** and gets a **doc-comment correction only**: its `.PARAMETER Force` promised the authorship meaning, but Microsoft Graph exposes no per-AU authorship, nothing ever emits a `Conflict` row, and its `'Conflict'` apply case is unreachable — so the help is made honest and the script is deliberately **not** given the switch. **Zero callers break, verified rather than asserted:** all ten `-Force` call sites against the six (`docs/getting-started.md`, `index.html`, `.github/prompts/deploy-unified.prompt.md`, `docs/solutions/unified-catalog/unified-catalog.md`) are **Export-path** (`-ExportCurrentState -Force`); apply-path `-Force` callers number **zero**. This also disposes of ADR 0052 §Alternatives-5, which rejected splitting `-Force` as "gratuitously breaking" because "four live CI call sites already pass `-Force` on the Export path" — those are Export-path, this split touches **only Apply**, and the parameter sets are **disjoint**, so the split ADR 0052 rejected is not the split this ADR makes. (A precision on "live", since repeating it uncritically would be the very error being corrected: those call sites are checked-in source that a downstream lab's CI *will* run **after kickoff step 5** — they have never executed *in this template repo*, whose workflows cannot get past `azure/login`.) **The template model makes ADR 0052's script-layer thesis stronger than ADR 0052 realised, and ADR 0053 now says so.** Per [ADR 0045](docs/adr/0045-template-kickoff-spinoff-model.md) and [ADR 0046](docs/adr/0046-tenant-placeholder-manifest.md) this repo is a **tenant-neutral template**: the `lab` Environment holds **zero secrets and zero variables by design** (`OWNER_APPROVAL_LOGIN` is repo-governance config, explicitly de-classified by ADR 0046 as *not* a tenant surface), tenant credentials arrive only at **kickoff step 5**, and until then `azure/login` cannot succeed — so **every tenant-touching workflow is inert**. The consequence ADR 0052 missed: the ADR 0029 **workflow-layer** typed-token gate sits inside a workflow that never reaches a reconciler, so in the template and in every fresh spin-off **it guards a code path that cannot execute** — while the **script-layer** gate needs *no repository secrets at all* (the operator's own `az login`, or the [ADR 0028](docs/adr/0028-co-equal-local-cert-credential.md) local-cert path) and is **fully live in a fresh clone on day one**. The asymmetry is the inverse of the intuition that a repo with no secrets cannot touch a tenant: **the only path that works is the only path the workflow gate cannot see.** ADR 0052 half-noticed this — it observed that `Deploy-FilePlan.ps1` has no workflow caller, so for *that* script the local terminal "is not one of two paths — it is the only path" — but treated it as a quirk of one reconciler. It is true of **every** reconciler in a pre-kickoff copy, and it is the state every consumer of this template starts in. So the split shipped here is not a refinement of a redundant protection; for a new consumer it is a correction to the **only** protection they have — a `-Force` that silently clobbers a foreign-authored object in a fresh copy has **no workflow gate behind it**. Making those inert workflows *skip* cleanly rather than fail is the complementary, independent [#91](https://github.com/marcusjacobson/Purview-as-Code/issues/91) (ADR 0054, number reserved). Finally, it documents (and explicitly **defers**) the fact that the `Conflict` **report category is itself overloaded** — `Deploy-EntraDirectoryRoles.ps1` and `Deploy-RoleGroupBackingEntraGroups.ps1` emit `Conflict` for a **shape** mismatch, not authorship, and the latter's `-Force` means a **third** thing again — so that #84's repo-wide guard test does not assert a single `Conflict` semantic that does not exist. Unblocks #83, which cannot roll `-Force` into `Assert-DestructiveOperationConfirmed` while `-Force` still means two things on six reconcilers. Tests are **verified non-vacuous**: all 64 new ADR 0053 assertions were replayed against pre-fix `1a37fbd`, where **46 go red** — including, on every Mechanism A script, "under `-Force` alone a foreign-authored drifted object EMITS a Conflict row and is NOT overwritten" — while the 18 that stay green are the deliberate both-sides guards (`-Force` still bindable in both parameter sets = the zero-callers-break proof, the Export-set negative, `ConfirmImpact = 'High'` on `UnifiedCatalogPolicies`, and AU not acquiring the switch) (#89)
- **ci:** bind `-Confirm:$false` on the four `-ExportCurrentState` reconciler steps that omitted it — `deploy-label-policies.yml`, `sync-label-policies-from-tenant.yml`, `deploy-auto-label-policies.yml`, and `sync-auto-label-policies-from-tenant.yml` — closing the gap left by the [ADR 0052](docs/adr/0052-destructive-confirmation-gate-at-script-layer.md) sweep, which covered only the three reference reconcilers (`Deploy-Labels.ps1`, `Deploy-DLPPolicies.ps1`, `Deploy-FilePlan.ps1`) and missed the two label-policy scripts entirely. ADR 0052 §Consequences makes the rule unambiguous — **every CI invocation of a reconciler binds `-Confirm:$false`** — and both `Deploy-LabelPolicies.ps1` and `Deploy-AutoLabelPolicies.ps1` wrap their `-ExportCurrentState` YAML write in `$PSCmdlet.ShouldProcess(...)`, so all four steps were in violation. **Today this is a latent no-op, not a live outage:** both scripts still declare `ConfirmImpact = 'Medium'`, PowerShell prompts only when `ConfirmImpact >= $ConfirmPreference`, and `$ConfirmPreference` defaults to `High` — so nothing prompts and nothing hangs *yet*. The defect is armed, not firing: the moment #83 raises those two scripts to `'High'`, all four steps would block forever on an interactive confirmation prompt no hosted runner can answer, converting a silent contract violation into a CI outage on a workflow that had been green. Fixing it here is therefore a **prerequisite that de-risks #83**, not a part of it — no `Deploy-*.ps1` is touched and `ConfirmImpact` stays at `'Medium'`. `-Force` does **not** already cover this, a trap worth naming because the steps *look* protected: in both scripts `$Force` is a plain `[switch]` consulted only at the export file-overwrite guard (`Deploy-LabelPolicies.ps1:1353`, `Deploy-AutoLabelPolicies.ps1:1189`) and never assigns `$ConfirmPreference` — unlike `Deploy-UnifiedCatalog.ps1:95-97`, which does disarm confirmation on `-Force`. That the omission was an oversight rather than a design choice is visible in the diff's own neighbourhood: in both `deploy-*.yml` files, three of the four invocations of the very same script (the `-WhatIf` enumerate pass, the audit plan pass, and the `repo-wins` apply pass) already bound `-Confirm:$false`, and only the `-ExportCurrentState` step missed it. Each fixed step carries an explanatory comment adapted from — deliberately **not** copy-pasted out of — the canonical ADR-0052 comment in `deploy-labels.yml`, whose "runs at `ConfirmImpact = 'High'` per ADR 0052" phrasing would be **factually false** on these two scripts today; the wording here is a forward-looking guard that is true at `'Medium'` and stays true once #83 lands, rather than an assertion of a present state that does not exist. The repo-wide guard test that would keep this class from silently reopening remains #84 (#87)
- **workflows:** retire the monolithic `deploy-data-plane.yml` and repoint every operator-facing reference, executing the retirement [ADR 0051](docs/adr/0051-per-solution-workflow-unit-of-data-plane-apply.md) gates. The file declared **32** `workflow_dispatch` inputs against GitHub's documented **25**-property cap, so it failed at *startup* before any job was scheduled — **90 runs, 0 successes, 0 jobs ever scheduled**, invalid since the scaffold commit `e30b51f` (2026-06-21). Deleting it is a **zero-regression** change: you cannot regress a workflow that has never once executed. The substance of this change is not the deletion but the **honesty sweep** of the ~46 operator-facing references that advertised an apply path which did not exist. The 5 surfaces that *do* have a per-solution workflow now point at it (`deploy-labels.yml`, `deploy-label-policies.yml`, `deploy-auto-label-policies.yml`, `deploy-dlp.yml`, `deploy-irm.yml`). The **12 that do not** — administrative units, Purview role groups, audit retention, retention/DLM, records/file plan, IRM entity lists, collections, glossary, classifications, data sources, scans, and unified catalog — now say so **plainly**, in one consistent reusable block: there is **no automated apply path yet**, the interim path is the local `scripts/Deploy-*.ps1` reconciler, and the backfill is tracked in #80. This is deliberate: quietly dropping the dead link would have been a *worse* outcome than the broken link it replaced, because it would hide the gap instead of naming it — ADR 0051 accepts the gap with eyes open, and the docs now speak in the same voice. Nothing was actually lost; those 12 surfaces never had a working apply path, only the **false appearance** of one. Also collects ADR 0051's Evidence 5 by reducing the `skip_names_irm` byte-lockstep note from **three-way to two-way** (`deploy-irm.yml` ↔ `sync-irm-from-tenant.yml`), since the third leg was the dead monolith; drops `deploy-data-plane` from the `github-actions.instructions.md` list of workflows required to declare `environment:`; and regenerates `index.html` via `scripts/Update-LandingPageEmbeds.ps1` rather than hand-editing it. The 11 superseded ADRs and the generated `docs/adr/README.md` index are left untouched — ADRs are immutable and a generated index cannot be superseded. Verified by a mechanical on-disk link check: **zero** dangling relative links to the deleted file remain, and every surviving `deploy-data-plane` mention is an immutable ADR, the generated ADR index, changelog history, or intentional prose about the retirement itself (#81)
- **ci:** close linked issues explicitly from `pr-auto-merge.yml`, completing the fix that the `issues: write` scope alone could not deliver. Verification on PR #76 proved the scope fix **insufficient**: `main` already carried `issues: write` at both label and merge time, the PR's trailer was `Refs #75` (so the body's `Closes #75` link was the sole mechanism under test, unconfounded), and `closingIssuesReferences` resolved correctly to `[{number: 75}]` — yet issue #75 received **no `ClosedEvent` of any kind**, identical to #64/#66/#68/#70/#72. The real root cause is broader than permissions: events produced by a `GITHUB_TOKEN`-attributed merge are suppressed outright ("events triggered by the `GITHUB_TOKEN` [...] will not create a new workflow run"), and in this repo that one rule has **three** confirmed faces — the implicit linked-issue close never fires; a `pull_request: closed` workflow never runs (`pr-stacked-retarget.yml` has fired only on the single human merge, #58, and on **none** of the 11 `GITHUB_TOKEN` merges); and a `push: [main]` workflow never runs (`validate.yml`'s push lane has exactly one run in its history, the same human merge). The close therefore **cannot** be hung off `pull_request: closed` or `push: main` — neither trigger ever fires for these merges — so the new `close-linked-issues` job hangs off the one non-suppressed trigger that already exists, the *human* `owner-approved` `labeled` event: it waits (bounded, 20 min) for `gh pr merge --auto` to actually land, reads GitHub's own authoritative `closingIssuesReferences` list, and closes each still-open issue with `gh issue close --reason completed`, citing the merging PR and its squash SHA. Idempotent (already-closed issues skip), fail-soft (an un-closable issue warns and continues rather than failing the run), and incapable of closing anything outside `closingIssuesReferences`. Also tightens the workflow to deny-by-default `permissions: {}` with per-job least privilege, which makes PR #74's `issues: write` load-bearing rather than dead — it now sits only on the job that closes issues, while the merge job holds no issue scope at all (#73)
- **agents:** route a confirmed **unified** Purview account to [`/deploy-unified`](.github/prompts/deploy-unified.prompt.md) instead of dead-ending it — `operator-tenant.agent.md` §1a.4 still told owners that classic `Deploy-*.ps1` onboarding was "blocked pending the ADR 0047 unified reconcilers", a statement that was true before those reconcilers shipped (`Deploy-UnifiedCatalog.ps1`, `Deploy-UnifiedCatalogPolicies.ps1`, `Get-PurviewAccountShape.ps1`, `/deploy-unified`) and is now simply stale; the true half is kept (classic `Deploy-*.ps1` / `/deploy-datamap` export-first onboarding still does not apply to a unified account) and the §1a.5 outcome matrix changes **only** in its "Also record" cell — the ADR 0048 placeholder rule ("leave the placeholder; do not write a classic name") and Hard rules 9/10 are untouched, because a tenant-level Unified Catalog has no ARM account name to write regardless of which reconcilers exist. Also refreshes the stale "once the ADR 0047 reconcile-time account-shape probe ships" parenthetical (it shipped as `Get-PurviewAccountShape.ps1`, and *corroborates* rather than replaces owner confirmation), adds the ADR 0048 discovery precondition to `/deploy-unified` — closing a one-way dangling contract where `/discover-purview-account` already declared itself a `/deploy-unified` precondition but was never pointed back to, and layering the ARM-plane "is this a real, non-decoy, owner-confirmed target?" gate ahead of the retained data-plane "which data plane does it speak?" shape gate — and indexes `/deploy-unified` in the `copilot-instructions.md` prompt list, where it was missing entirely (#75)
- **ci:** add the missing `issues: write` scope to the `permissions:` block in `pr-auto-merge.yml`. A top-level `permissions:` block is exhaustive — every scope not listed is set to `none` — so declaring only `pull-requests: write` and `contents: write` left `issues` at `none`, and the merging identity (`secrets.GITHUB_TOKEN`, via `gh pr merge --squash --auto`) had no permission to close the linked issue. Five consecutive PRs (#64/#66/#68/#70/#72) left their linked issue open with **no** `ClosedEvent` on the timeline at all, while `closingIssuesReferences` resolved correctly on every one — so the link was never the defect, the merging identity's scope was. This adds the missing scope; it is **not** yet confirmed to fix auto-close, because `GITHUB_TOKEN`-raised events are independently known not to trigger downstream automation regardless of scope. Verification is pending on the next merged PR, and issue #73 stays open until a real `ClosedEvent` is observed (#73)
- **docs-regen:** make the ADR-index generator authoritative per ADR 0050 — repair the status regex (`docs-regen.yml:86`), whose lazy quantifier terminated on the first whitespace character and collapsed `Superseded by [ADR NNNN](…)` to a bare `Superseded` (ADRs 0008 and 0037), so it now captures the full status up to an inline HTML comment or end of line; repair the title-strip regex (`docs-regen.yml:80`), which matched the em dash only and leaked the ADR number into the `Title` column for ADR 0039 (`-`) and ADR 0042 (`--`), so it now tolerates em dash, en dash, `--`, and `-`; anchor the table replacement on the table header row so prose between the `## Current ADRs` heading and the table survives regeneration; migrate the curated index prose for ADR 0044 (`watch-list.yml`, fired trigger #1) and ADR 0045 (the KickoffGuard ratification list, plus the `Tracks #4` the curated row silently dropped) into those two ADRs' own `Gates:` lines so the first clean regeneration is lossless; rewrite `docs/adr/README.md` process step 4, which told authors to hand-edit the table and contradicted both ADR 0050 and `.github/skills/docs-maintenance/SKILL.md`; add the "do not hand-edit" NOTE callout above the table; and land the first generator-clean regeneration of the 46-row ADR index (which fixes the hand-curated ADR 0049 row's factually wrong "`sync-*-from-tenant` pair" — its source `Gates:` line already correctly enumerates all three workflows) together with the regenerated `docs/scripts-reference.md`, picking up 7 missing scripts and the stale `Deploy-UnifiedCatalog` placeholder synopsis (#69)

### Documentation

- **agents:** port the "Cursor Cloud specific instructions" section into [AGENTS.md](AGENTS.md) from the unlanded Cursor Agent commit `0f34608` (stray branch `cursor/setup-dev-environment-f969`, never merged or rebased — content ported fresh), re-deriving the validate-gate enumeration from [`.github/workflows/validate.yml`](.github/workflows/validate.yml): the original's "same seven gates" phrasing is replaced by the current **ten** jobs (`bicep`, `yaml`, `powershell`, `actionlint`, `identifier-residue`, `pester`, `dspm-posture`, `dspm-ai-posture`, `full-circle-contract-guard`, `landing-page-embeds`) with their local-equivalent commands, the `yaml` gate's target updated to `data-plane/ examples/` per ADR 0056, the Pester 5.x-never-6.x pin guidance and live-tenant caveats preserved, and the unverifiable Cursor-VM-snapshot claims qualified as environment configuration maintained outside the repo (#121)
- **adr:** add ADR 0051 — the per-solution workflow (`deploy-<solution>.yml`) is the unit of data-plane apply, and the monolithic `deploy-data-plane.yml` is retired; each per-solution workflow owns exactly one data-plane surface, with `push:` path triggers, a small `workflow_dispatch` input surface, `permissions: {}` deny-by-default with least-privilege per-job splits, a concurrency group, and (where the surface supports export) two-pass deterministic skip enumeration and automated drift-back PRs. Records the evidence that the monolith is **structurally invalid and has never once executed**: it declares **32** `workflow_dispatch` inputs against GitHub's documented **25**-property cap, has done so since the original scaffold commit (`e30b51f`, 2026-06-21), and its Actions history is **90 runs, 0 successes, 0 jobs scheduled** — the runs are even attributed to `push` despite the file declaring no `push:` trigger, the signature of an `on:` block that never parses. Deletion is therefore a zero-regression change. Also records the five-workflow precedent (PRs #58–#61, #70), the rationale already written into `deploy-irm.yml` lines 12–17 (quoted verbatim), the fact that the monolith is a **stale subset rather than an umbrella** (`deploy-label-policies.yml` and `deploy-auto-label-policies.yml` cover surfaces it never had), and the three-way `skip_names_irm` byte-lockstep tax it levies while dead. Corrects the intake's "hard limit of 10" — that is the `repository_dispatch` `client_payload` cap, not the `workflow_dispatch` `inputs` cap of 25; the conclusion is unchanged, since 32 exceeds both the current 25 and the historical 10. Consequences: 12 of ~14 surfaces have **no** automated apply path (they never did — the monolith only made it *look* like they had one), so the documented path for those is the local `scripts/Deploy-*.ps1` reconciler until backfilled; supersedes the relevant passages of **11** ADRs (0003, 0010, 0011, 0021, 0026, 0035, 0036, 0037, 0038, 0046, 0049) while leaving them otherwise standing and unedited — the 12th `grep` hit, `docs/adr/README.md`, is a **generated** index under ADR 0050 and cannot be superseded; and a `deploy-all.yml` orchestrator is explicitly deferred as greenfield `workflow_call` work. Rejects collapsing the inputs to fit the cap (valid but always-red, and **worse than absent because it looks like it should work**) and reducing the file to an orchestrator (no coherent contract to orchestrate at 3 of ~14 surfaces). Gates the follow-up that deletes the workflow and sweeps the operator docs (#78)
- **adr:** add ADR 0050 — the `docs/adr/README.md` "Current ADRs" table is machine-generated by `docs-regen.yml` and is never hand-edited; each ADR's three header lines (`# NNNN — Title` H1, `- **Status:**`, `- **Gates:**`) are the generator's only inputs and therefore the single source of truth for that ADR's row, and must be correct on merge; records the 2026-07-13 evidence (0 of 45 committed rows generator-clean; curated titles are unreproducible editorial rewrites; the curated ADR 0049 row is factually wrong about its own source), ratifies `.github/skills/docs-maintenance/SKILL.md`, and gates the follow-up item that fixes the two generator defects (status truncation at `docs-regen.yml:86`, title number leak at `docs-regen.yml:80`), migrates the ADR 0044 / 0045 curated prose into their `Gates:` lines, and corrects `docs/adr/README.md` process step 4 (#67)
- **adr:** ratify ADRs 0044, 0045, and 0046 to `Accepted` (each one's decision has shipped), resolve the duplicate 0045 number by moving the misfiled tracking plan out of the ADR namespace to `docs/plans/adr-0045-implementation-plan.md` (re-pathing its four sibling-ADR links), and backfill the `docs/adr/README.md` index with the missing rows for 0044, 0045, 0046, and 0049 — 0049's authored `Proposed` status is left untouched pending a separate owner ruling (#63)

### Tests

- **scripts:** close **B2**, the documented hole in the ADR 0052 plan-keying guard PR #102 shipped. That guard proves no gate's **condition** mentions `$DirectionPolicy`. B2 satisfies it and is still fatal: leave the condition honestly plan-keyed and launder the policy through the **list the condition counts** — `if ($DirectionPolicy -ne 'repo-wins') { $repoWinsOverwrites.Clear() }` — so the gate sits silent while the writes proceed. **It is the *natural* mistake, not an adversarial one:** seven ungated Class A reconcilers already carry a live `$DirectionPolicy -eq 'repo-wins'` test that today wraps only a `Write-Warning`, so an author rolling out PR-B by pattern-matching the local idiom drops the `.Add()` **inside that existing `if`** and writes B2 *by following the surrounding code*. **This blocks #83's PR-B, which is 17 fresh chances to write it.** The guard rests on one structural fact: **B2 is possible only where a gate's list DIVERGES from the write loop's source.** The **overwrite** gate keys on a hand-maintained **shadow list** of display strings, separate from the `$plan` that drives the `Set-*` writes — empty the shadow and the writes still fire. That is the entire exposure. The **prune** gate is immune **by construction** — its list either *is* the delete loop's source (`Deploy-Labels.ps1`'s `$orphans`, so emptying it cancels the deletes too — which is why `$orphans = @()` in the audit short-circuit is correct and not a defect) or is derived from the plan one line above the gate with zero lifetime. Three AST rules, all deriving the list **from the gate** (never hardcoding a name, which would silently stop applying the moment PR-B renames a list): **(a)** the overwrite list is **APPEND-ONLY** — no `.Clear()`, no reassignment, no aliasing, and a **whitelist** of cannot-shrink methods rather than a blacklist of `Clear|Remove*`, which would fail *open* on `HashSet.ExceptWith` or a novel collection type. **Absolute, no policy carve-out** — the originally-agreed rule fired only on shrinks under a *policy-keyed* condition and therefore let an **unconditional** `.Clear()` straight through, which is exactly as silent and exactly as fatal. **(b)** the list's **population may not discriminate between two WRITING policies**, stated as a **whitelist**: the only permitted policy test is one whose **truth set**, computed by *evaluating* the condition over the script's own `[ValidateSet]` values, is exactly `{portal-wins, repo-wins}`. `-ne 'audit'` passes; `-eq 'repo-wins'` (`{repo-wins}`) and `-ne 'portal-wins'` (`{audit, repo-wins}`) are findings, and **any condition whose truth set cannot be computed is a finding** — a blacklist fails *open* on a novel spelling and this repo has been burned by fail-open guards. **The carve-out, stated once and used by both (b) and (c): `audit` is the sanctioned NON-WRITING mode; `portal-wins` and `repo-wins` BOTH write** — anchored to `[ValidateSet('audit','portal-wins','repo-wins')]`, re-derived from each script's own param block on every run, so a **fourth** policy value fails loudly and demands an owner ruling instead of silently widening the whitelist (the ADR 0056 "carve-out with a stated reason, mechanically re-verified" idiom). **(c)** the **prune** list is append-only too, pinning the immunity (a)/(b) rely on, with a **single** declared exception — `Deploy-Labels.ps1`'s `$orphans = @()` — whose reason is **re-verified against source**: it holds only while that block is gated on `audit` alone **and** also empties the write plan (`$plan.Clear()`). Delete that line and rule (c) goes **RED**. *A carve-out without a mechanically-asserted reason is the erosion this suite exists to prevent.* Three fail-open holes were found **by attacking the guard after building it**, and all three are closed: the policy laundered through a **`continue` guard clause** (a *sibling* of the `.Add()`, not an ancestor — and the guard-clause idiom is the one these scripts already use for every plan filter, making it the **likeliest** way PR-B actually writes B2); the list handed to a **locally-defined function** that clears it (a .NET list is a reference type); and a `$DirectionPolicy` **entangled with another variable**, which dissolved harmlessly into the truth-set intersection until the guard was taught to fail closed on it. **Eight mutants injected into the real `Deploy-Labels.ps1`, each confirmed RED with the correct rule firing, then reverted byte-identically** — and the red replay is made **permanent**: 12 mutant shapes and 5 correct shapes run against synthetic ASTs through the *same* analyzer on every CI run, so a future no-op refactor of the guard goes red rather than silently reporting zero forever. **B1 (policy laundered through a local variable) REMAINS UNCAUGHT and is honestly documented as an open boundary** — catching it needs data-flow analysis. *An honestly-labelled limited guard is fine; a limited guard sold as complete is not.* (#103)

## 2026-07-12

### Fixed

- **scripts:** redact the real Entra tenant-scoped GUID from the `IRM_Tenant_Setting_<tenant-guid>` policy name across all IRM artifacts (skip-baseline defaults, YAML header, smoke-test baseline, ADR 0036, runbook, solution guide) — a tenant-scoped GUID is a real tenant identifier prohibited by the "Environment and identifier boundaries" rule, which supersedes ADR 0036 §Security #1 — and drop the now-redundant system-managed skip entry, since `Deploy-IRMPolicies.ps1` already classifies any `IRM_Tenant_Setting_*` policy as `NoChange` via a name-prefix wildcard (skip baseline shrinks 5→4 operator-authored names) (#60)
- **scripts:** make `Deploy-AutoLabelPolicies.ps1` round-trip exportable state — `-ExportCurrentState` now builds rules first and skips any whose resolved `contentContainsSensitiveInformation` is empty (EDM / trainable classifier / document fingerprint), then skips parent policies left with zero surviving rules, reporting both as skipped orphans instead of emitting entries that fail the CCSI `minItems:1` floor on the next deploy (ADR 0016 §12) (#57)
- **policies:** remove the `minItems: 1` floor on `exchangeLocation` in `auto-label-policies.schema.json`, and relax the reconciler's forward-apply input guard to require the key present but allow an empty array, so a SharePoint/OneDrive-only policy's `exchangeLocation: []` round-trips; empty-location writes are gated (Create omits `-ExchangeLocation`, Update skips it) so desired `[]` == tenant `[]` → NoChange (ADR 0016 §12) (#57)

### Documentation

- **docs:** document the IRM reverse drift-detection workflow (`sync-irm-from-tenant.yml`) and its Tier-3 issue-based (not PR-based) semantics in the Insider Risk Management solution guide and the IRM end-to-end smoke runbook (#59)
- **docs:** point the DLP solution guide and end-to-end smoke runbook at the new `deploy-dlp.yml` / `sync-dlp-from-tenant.yml` workflows and drop the "no dedicated forward workflow" gap (#70)
- **docs:** add ADR 0016 §12 (export-scope exclusion + NoChange-only location semantics), a round-trip/scope section in the auto-label-policies solution guide, and export-scope notes in the YAML header (#57)

### CI/CD

- **ci:** add IRM isolated forward companion `deploy-irm.yml` — a Tier-3 (export-incapable) single-apply-pass forward workflow for the Insider Risk Management surface that runs `Deploy-IRMPolicies.ps1` with the ADR 0029 direction-policy contract (dispatch inputs, repo-wins typed-confirm gate, `audit` mode, push-uses-defaults, `concurrency`, KV firewall window); forward twin of `sync-irm-from-tenant.yml`, mirroring the `deploy-dlp.yml` precedent, with no two-pass enumerate (static ADR 0036 skip baseline), no drift-back PR (reverse is issue-based), and no verify-published step; adds a Tier-3 carve-out to the ADR 0029 companion-workflow contract in `.github/instructions/github-actions.instructions.md` (#59)
- **ci:** add IRM reverse drift-detection companion `sync-irm-from-tenant.yml` — a Tier-3, issue-based (not PR-based) reverse leg for the Insider Risk Management surface that runs `Deploy-IRMPolicies.ps1 -DirectionPolicy audit` (read-only, ADR 0029), detects drift from the reconciler's returned object rows (`.Category`/`.Name`/`.Reason`, no stdout scraping), post-filters the ADR 0036 skip baseline, and opens a GitHub issue with self-provisioned labels; closes the reverse-leg gap for IRM per the companion-workflow rule in `.github/instructions/github-actions.instructions.md` (#59)
- **ci:** add DLP companion workflows `sync-dlp-from-tenant.yml` (scheduled reverse drift-back, `-ExportCurrentState`) and `deploy-dlp.yml` (isolated forward apply with the ADR 0029 enumerate/apply/drift-back direction-policy contract), closing the Tier-1 loop for the Data Loss Prevention surface per the companion-workflow rule in `.github/instructions/github-actions.instructions.md` (#70)

## 2026-07-11

### Fixed

- **scripts:** guard the `None` "no default label" sentinel in `Resolve-DesiredAdvancedSettingLabel` (Deploy-LabelPolicies.ps1) so `OutlookDefaultLabel`/`DefaultLabel`/`teamworkdefaultlabelid` set to any casing of `none` normalize to the lowercase tenant sentinel instead of Blocking as an unresolved label reference; adds Pester coverage (#55)
- **policies:** remove the `minItems: 1` floor on `exchangeLocation` in `label-policies.schema.json` so a group-scoped-only label policy's `exchangeLocation: []` (emitted by `-ExportCurrentState`) passes forward-deploy schema validation (#55)
- **infra:** grant the data-plane automation SP `Key Vault Contributor` at vault scope so the single-login data-plane firewall-toggle workflows can run `az keyvault update --public-network-access Enabled` (management-plane `Microsoft.KeyVault/vaults/write`); the prior `Key Vault Crypto User`-only grant did not cover it (ADR 0049) (#53)

### Documentation

- **docs:** add ADR 0049, update the automation-identity guide's 5d RBAC section, and add `drift-detection.yml` to the "Allow GitHub Actions to create and approve pull requests" rationale in the CI repo-settings runbook (#53)

## 2026-07-09

### Added

- **prompts:** add `/deploy-unified` prompt for the Unified Catalog track — account-shape gate via `Get-PurviewAccountShape.ps1`, first-run export-first check, then a concepts pass and a grant/revoke-aware policies pass, each its own `-WhatIf` -> confirm -> apply cycle (ADR 0047 item e) (#51)
- **scripts:** add `Get-PurviewAccountShape.ps1` for read-only classic-vs-unified account-shape detection, plus Pester coverage and Unified Catalog guide updates (ADR 0047 item d) (#49)
- **scripts:** add `Deploy-UnifiedCatalogPolicies.ps1` with grant/revoke-aware gating for Unified Catalog data-access policies, plus tests and guide updates (#47)

### Documentation

- **prompts:** update `/discover-purview-account` Step 4/5 to route a confirmed unified account to `/deploy-unified` instead of stopping, and to mention the shipped `Get-PurviewAccountShape.ps1` probe as corroboration for the owner-confirmed classic-vs-unified determination (#51)

## 2026-07-08

### Added

- **unified-catalog:** promote `Deploy-UnifiedCatalog.ps1` to a live full-circle reconciler for business domains, data products, OKRs, critical data elements, and glossary terms (#45)
- **unified-catalog:** rename `data-plane/unified-catalog/governance-domains.{yaml,schema.json}` to `business-domains.{yaml,schema.json}` to match the `2026-03-20-preview` Unified Catalog API's Business Domain operation group, and add `glossary-terms.{yaml,schema.json}` (Terms operation group) and `data-access-policies.{yaml,schema.json}` (Policies operation group, modeled as a simplified role-assignment projection pending the dedicated grant/revoke-aware reconciler) — schema-only scaffolding per ADR 0047 §Decision item 5/10, item (a); no reconciler logic or live API calls added (#43)
- **scripts:** add an opt-in, read-only `-ProbeUnifiedCatalog` switch to `Find-PurviewAccount.ps1` — when ARM enumeration finds no *confirmed* governance account (an empty result, or a result made up entirely of `RequiresOwnerConfirmation` hits such as a pay-as-you-go metering resource), runs a single tenant-scoped GET against the Unified Catalog preview data-plane `businessdomains` enumerate endpoint (`2026-03-20-preview`) and appends a diagnostic classification (`UnifiedCatalogTenantReachable`, `UnifiedCatalogUnauthorized`, `UnifiedCatalogProbeIndeterminate`, `UnifiedCatalogUnreachable`, `UnifiedCatalogProbeSkipped`) to the returned array; default off, never writes, never prints the token, and its diagnostic label must never be written to `purviewAccountName`; Pester coverage of the classification, shaping, and probe-calling functions, including a regression guard proving the probe still fires when ARM surfaces only a metering resource (#41)
- **instructions:** wire the opt-in Unified Catalog tenant-reachability probe into the ADR 0048 operator flow — `@operator-tenant` Step 1a.3 and the `/discover-purview-account` prompt Step 3 now offer `Find-PurviewAccount.ps1 -ProbeUnifiedCatalog` to corroborate the "tenant-level Unified Catalog" hypothesis in the "not found in ARM" branch, with an explicit never-write-to-`purviewAccountName` rule (#41)

### Documentation

- **docs:** update `docs/solutions/unified-catalog/unified-catalog.md` and ADR 0024 with a non-substantive note reflecting the ADR 0047 rename and the two new concept files (#43)
- **docs:** add an ADR 0048 addendum grounding the opt-in Unified Catalog tenant-reachability probe in the Learn-documented `businessdomains` enumerate operation, framed conservatively as a tenant-level reachability signal that neither confirms a specific account's type nor reopens item 5's classic-vs-unified caveat (#41)

## 2026-07-06

### Added

- **prompts:** add the read-only `/discover-purview-account` prompt implementing the ADR 0048 deploy-time discovery-and-confirmation gate — enumerate `Microsoft.Purview/accounts` for the `purviewAccountName` in `infra/parameters/lab.yaml` across every visible subscription via `Find-PurviewAccount.ps1`, present each hit with the verbatim pay-as-you-go-metering warning, handle "not found in ARM" (unified / other-subscription / not-yet-created) as first-class, confirm classic-vs-unified, and report the outcome matrix; stop before `Connect-Purview.ps1` on any unconfirmed / metering-only / unified target instead of reconciling; `/deploy-datamap` gains a matching precondition pointing at the gate (#35)
- **instructions:** turn `@operator-tenant` Step 1 Q8 into a discover-then-confirm gate (new Step 1a) implementing ADR 0048 — run read-only discovery (`Find-PurviewAccount.ps1`, or `az account list` + per-subscription `az resource list`) across every visible subscription, present each hit with the verbatim pay-as-you-go-metering warning, handle "not found in ARM" (unified / other-subscription / not-yet-created) as first-class, record and route on classic-vs-unified, and leave the `purview-contoso-lab` placeholder with an "account unconfirmed — owner action required" note rather than write a guessed name; `tenant-placeholders.yaml` `purviewAccountName` note updated to reference the gate (#37)
- **scripts:** add read-only `Find-PurviewAccount.ps1` discovery helper implementing the ADR 0048 gate — enumerate `Microsoft.Purview/accounts` across every visible subscription (`az account list` → per-subscription `az resource list`), return one structured object per hit (name, resource group, region, sku) with a `RequiresOwnerConfirmation` classification and the pay-as-you-go-metering warning, and emit a first-class "not found in ARM" result rather than an error; Pester coverage of the shaping and `az`-wrapping functions (#36)

### Documentation

- **docs:** add ADR 0048 requiring `@operator-tenant` to run a read-only discovery-and-confirmation gate for the Purview account target — enumerate `Microsoft.Purview/accounts` across every visible subscription, distinguish a governance account from a pay-as-you-go metering decoy, handle the "not found in ARM" (unified / other subscription / not-yet-created) case as first-class, route on classic-vs-unified, and never write a guessed account name — complementing ADR 0047's reconcile-time routing (#34)
- **docs:** record the Microsoft Purview Unified Catalog preview-REST-API coexistence decision in ADR 0047, superseding ADR 0037 after its watch-list trigger #1 fired (#32)

## 2026-07-05

### Changed

- **instructions:** single-source the `@operator-tenant` Step 6 placeholder scans from `tenant-placeholders.yaml` — promote `residualScan` / `functionalWorkflowScan` to structured fields (`schemaVersion` 2), add the `Get-TenantResidualScanCommand.ps1` generator and a manifest-based fallback in the agent body, and drop the removed owner-login clause from the functional scan (#29)

### Fixed

- **scripts:** `Test-KickoffGuard.ps1` exits `0` on a passing guard instead of leaking a non-zero `$LASTEXITCODE` from the last internal `git` call, restoring exit-code gating; add Pester coverage of the exit-code contract (#27)
- **instructions:** operational deploy/add prompts (`deploy-infra`, `deploy-datamap`, `add-classification`, `add-data-source`, `build-item`) read the resource group, region, Purview account, and tenant domain from `infra/parameters/lab.yaml` instead of embedding tenant literals that break on a tailored copy (#27)
- **instructions:** `tenant-placeholders.yaml` closes two scan-only tenant surfaces — the `.github/CODEOWNERS` header slug (`ownerSlug`) and the `.github/copilot-instructions.md` Squad owner-identity line (`githubOrg`) — and `@operator-tenant` Step 5 is synced (#27)

### Documentation

- **docs:** rewrite `getting-started.md` §4 to lead with the export-first onboarding bootstrap (`-ExportCurrentState -Force`, per domain) before reconciling with `-WhatIf`/apply, so a first run against a pre-existing account no longer surfaces every live object as `Orphan` drift; cross-link the `/deploy-datamap` prompt (now with a first-run precondition) and name export-first onboarding as `@operator-tenant`'s post-tailoring next step (#30)
- **docs:** add a "Teardown / re-run" section to the kickoff guide — rebuilding requires manual GitHub-UI repo deletion because the automation token lacks the `delete_repo` scope by design (#27)
- **docs:** record the prompt-decoupling and closed-tenant-surface decisions in ADR 0046 (#27)

### Tests

- **scripts:** add drift-guard Pester coverage for `Get-TenantResidualScanCommand.ps1`, asserting the generated residual / functional scans stay faithful to `tenant-placeholders.yaml` (#29)

### CI/CD

- **ci:** make the `idea-intake-autoadd.yml` needs-review owner gate data-driven via the `OWNER_APPROVAL_LOGIN` repository variable (two-job least-privilege split; warns and skips on unset instead of failing), removing the hardcoded owner login (#29)

## 2026-07-04

### Added

- **instructions:** add tenant placeholder manifest (`.github/agents/tenant-placeholders.yaml`) — ordered longest-match-first tokens, the authoritative tenant-surface edit list, the intentional-sample allowlist, and de-template markers — for deterministic, corruption-safe template tailoring (#24)

### Changed

- **instructions:** wire `@operator-tenant` to the placeholder manifest — ordered replacement, intentional-sample-scoped residual scan, diff-scoped secrets scan, targeted functional-workflow scan, and a de-template step; add custom-domain, missing-origin, and harness-portability notes (#24)
- **instructions:** fix `@operator-kickoff` `isTemplate` false-positive (a template clone no longer reads as the canonical template), flag local-workspace mode as a CI/CD dead-end, and reconcile `origin` removal with `@operator-tenant`'s org/repo defaults (#24)
- **instructions:** ban em-dashes / ellipsis / smart quotes in interactive menu labels (INTERACTION-MENUS.md + agents.instructions.md) and convert existing agent menu labels to ASCII (#24)

### Fixed

- **docs:** correct getting-started OIDC identity setup to the per-plane app trio (`gh-oidc-purview-control-plane` / `-data-plane` / `-kv-unlock`) with `:environment:<env>` federated-credential subjects — the previous single-app `gh-Purview-as-Code-Generic` with a `:ref:refs/heads/main` subject would fail `azure/login` (#24)

### Documentation

- **docs:** add ADR 0046 — tenant placeholder manifest for template tailoring (#24)

## 2026-07-03

### Added

- **scripts:** add `Update-LandingPageEmbeds.ps1` to refresh (and `-Check`) the offline documentation snapshots embedded in `index.html` (#22)
- **scripts:** resolve the `@operator-kickoff` source template URL via the GitHub template relationship (with `origin` fallback), so the no-push-back guard works after "Use this template" and never targets the consumer's own repo (#18)
- **scripts:** add the `@operator-kickoff` kickoff agent and the no-push-back guard (`scripts/modules/KickoffGuard.psm1`, `Set-KickoffGuard.ps1`, `Test-KickoffGuard.ps1`) that severs a template copy from the source repository, with Pester coverage (#7)

### Changed

- **repo:** add `tests/**` to the `@artifact-resolver` authorized file scope (#16)
- **repo:** mark the source repository as a GitHub template so consumers use "Use this template" for spin-off copies (#9)
- **docs:** add ADR 0045 — template kickoff and spin-off consumption model with a no-push-back guard (#4)

### Fixed

- **ci:** make the pr-auto-merge owner gate data-driven via the `OWNER_APPROVAL_LOGIN` repository variable, removing the hardcoded owner login (#6)

### Documentation

- **docs:** add a browser-openable HTML landing page (`index.html`) with a slide-in side panel that renders the linked Markdown docs in-page (#22)
- **docs:** add an opinionated kickoff guide (`docs/kickoff-guide.md`) and feature it as the "start here" entry point in the README (#20)
- **docs:** rewrite the README quick-start and tenant-onboarding guide to lead with the `@operator-kickoff` decouple step before tenant intake (#8)
- **docs:** add ADR 0045 implementation tracking plan sequencing the follow-on tasks #6–#9 (#10)

### Tests

- **scripts:** add Pester coverage for `Update-LandingPageEmbeds.ps1` (embedding, drift detection, `-WhatIf` safety, idempotency) (#22)

### CI/CD

- **ci:** add a `validate.yml` gate that fails when `index.html`'s embedded doc snapshots drift from their source docs (#22)

## 2026-06-21

### Added

- **repo:** initial generic Purview-as-Code template baseline — every tenant-specific identifier replaced with Microsoft's documented placeholders (`contoso`, `contoso.onmicrosoft.com`, zero-GUID), and the `@operator-tenant` Tenant Intake agent added for per-tenant tailoring.

### Changed

- **docs:** reset `docs/project-plan.md` to a generic, empty roadmap template (the §3/§5/§6/§8 framework is preserved so the Squad agents still resolve their gates).

### Fixed

- **scripts:** restore the UTF-8 BOM on 15 PowerShell files that lost it during the genericization token-rewrite, fixing the `PSUseBOMForUnicodeEncodedFile` PSScriptAnalyzer failure (#1).

### Removed

- **docs:** remove lab-only build-up and test working docs — the archived v1 plan, the wave-0 smoke-test log, the repo-consistency-housekeeping and role-group-backing audits, and the agentic-process-modernization plan (references repointed to surviving canonical docs).

### Documentation

- **docs:** add a README quick-start and a detailed [`docs/tenant-onboarding.md`](docs/tenant-onboarding.md) guide for the clone → `@operator-tenant` → secrets/OIDC → deploy flow (#1).
